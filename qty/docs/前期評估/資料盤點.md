# LoopFlow QTY — 資料盤點

> 本文件是 QTY **模型與字典資料現況**的唯一來源。範圍見 [`範圍與邊界.md`](./範圍與邊界.md)，量測方式見 [`量測方法.md`](./量測方法.md)。
>
> 建立日期：2026-09-01　｜　**全部數字為實測**，非推估

本文件回答一個問題：**QTY 需要的資料，模型與字典到底有沒有給？**

## 0　驗證模型

| 項目 | 內容 |
|---|---|
| 檔案 | `LoopFlow_R_MCP.rws`（worksession） |
| 組成 | `LoopFlow_3D_R_MCP.3dm` **attach 進** `LoopFlow_2D_R_MCP.3dm` |
| 位置 | `<LOOPFLOW_QTY_MCP_WORKFILES_ROOT>`，雙機路徑見工作區 `工作檔路徑.md` |
| 字典 | `LoopFlow_Dictionary.xlsx`，與 `.3dm` 同層（Nexus 引用，LoopFlow 既定結構） |
| 專案設定／Registry | 2026-09-02 已補齊並遷到 2.0.7 路徑，見 §9 |
| 模型單位 | **Centimeters**，絕對公差 0.1 |

**這是依實際作業方式配置的環境**，不是簡化的測試檔。

## 1　物件總覽

| 分類 | 數量 |
|---|---|
| 完整列舉（含隱藏／鎖定／參照） | **3116** |
| 　本機側（2D 圖面，作用中文件） | 2972 |
| 　**參照側（3D 模型，attach 進來）** | **144** |

### 1.1 幾何型別分布

| 側 | 型別 | 數量 |
|---|---|---|
| 本機 2D | Curve、Annotation、Hatch、Block、Detail | 2972 |
| **參照 3D** | **Brep** | **39** |
| 參照 3D | InstanceReference（圖塊） | 少數 |
| 參照 3D | **Point（放置點）** | **約 97** |
| 參照 3D | Curve（空間框） | 2 |

**所有可量測的 3D 實體都在參照側，本機 2D 側一個 Brep 都沒有。**

## 2　標記狀況

### 2.1 帶 2.0 身分欄的物件

| 側 | 帶 `_03_ID編號` | 組成 |
|---|---|---|
| 本機 2D | 61 | 全部是 Curve |
| **參照 3D** | **37** | Brep 30 ＋ Block 7 |
| 合計 | 98 | |

UserText 為真實資料，例如：

```text
_01_空間名稱   廊道
_02_建構狀態*  New
_03_ID編號     WL-04
_05_高程基準   BH
_07_UUID       ce16eeba-…
_09_空間ID     8c6e6e16-…
_11_類型類別   WL
_12_類型序號   04
_14_資料版次   4
```

### 2.2 `_07_UUID` 與 3D 物件是 1:1

| 每個 UUID 的組成 | UUID 數 |
|---|---|
| 1 個物件 | 13 |
| 2 個物件 | 11 |
| 3 個物件 | 4 |
| 4 個物件 | 6 |
| 5／7／15 個物件 | 各 1 |

組成模式：

```text
LCurve + RBrep                        x11
RInstanceReference                    x7      ← 無 2D
LCurve×3 + RBrep                      x6
RBrep                                 x6      ← 無 2D
LCurve×2 + RBrep                      x4
LCurve×14 + RBrep                     x1      ← 一項畫了 14 條線
```

**每個 UUID 恰好有一個 3D 代表**（30 Brep ＋ 7 Block ＝ 37），2D 則是一對多、最多 14 條。

**推論**：只算 3D 時，`_07_UUID` 與 3D 物件為 1:1，`object_id` 與 UUID 在 QTY 內可互換，「一個物件」的粒度問題不存在。

### 2.3 2D 與 3D 由 `_07_UUID` 綁定

| | 相異 UUID |
|---|---|
| 本機 2D | 24 |
| 參照 3D | 37 |
| **共用** | **24（本機全部在 3D 側找得到）** |

3D 側另有 13 個 UUID 沒有 2D 對應（並非每個 3D 物件都需要平面符號）。

**但 2D 不採用**（見 §3）。此節僅記錄綁定關係存在，日後若需要回頭關聯 2D 與 3D 時可用。

## 3　為什麼 2D 不採用

**計算標的一律是 3D 模型，所有 curve 直接略過。**

理由：2D 線稿是**出圖用**，而且是**從 3D 切出來的**，切出後還會做後續修改。因此 2D 與 3D 之間隨時可能不同步，不能作為計算依據。

實測也顯示 2D 覆蓋不完整——37 個項目裡只有 24 個有 2D 曲線，缺的 13 個包含全部 7 個 `COUNT` 類（電器、衛浴），那些本來就只要數不要量。

> 若開發過程中出現「一定得要有 curve 輔助計算」的情形，另議。

**2D 曲線的品質本身沒有問題**：實測 61/61 全部封閉且共平面，面積都算得出來。不採用是資料同步的考量，不是品質問題。

## 4　計算全集的定義

Coverage 檢核的「全部物件」**不是**文件中所有物件：

| 類別 | 數量 | 納入全集 |
|---|---|---|
| 帶 `type_id` 的 Brep／Block | 37 | ✅ |
| 結構體（`00_STR_*`）的 Brep | 16 | ✅ 但另計（§6） |
| **Point（放置點）** | 約 97 | ❌ **排除** |
| **Curve** | — | ❌ **排除** |

**若不排除 Point，那 97 個放置點會全部落入「未選取」，Coverage 一開跑就被雜訊淹沒。**

全集定義：**3D 側的 Brep 與 InstanceReference**。

## 5　字典 join：完整可用

以 `type_id` 查 `LoopFlow_Dictionary.xlsx`（92 筆 Type）：

| 檢查 | 結果 |
|---|---|
| 相異 `type_id` | 26 |
| 查得到 `measurement_rule` 與 `estimation_unit` | **26 / 26** |
| `classify_measurement` 量綱一致性 | **26 / 26 `pass`** |
| 以物件計 | **98 / 98** |

抽樣：

| type_id | 顯示名稱 | rule | unit |
|---|---|---|---|
| `FL-02` | 地毯 | `AREA_WD` | 坪 |
| `WL-04` | 裱布皮革 | `AREA_WH` | 坪 |
| `CB-06` | 櫃體石材面 | `AREA_WH` | 才 |
| `CB-01` | 木作櫃體 | `LEN_W` | mm |
| `WL-16` | 鋁製收邊條 | `LEN_W` | cm |
| `EL-05` | 插座面板 | `COUNT` | 組 |

**結論**：輸入合約完整，`初步評估報告.md` §7.1「輸入合約八月已寫定，不需重新設計」獲得證實。**規則不需要在 GH 重新輸入。**

### 5.1 規則分布與幾何對應

| 規則 | 項目數 | 幾何型態 |
|---|---|---|
| `COUNT` | 7 | 全部是 Block（電器、衛浴、消防） |
| `AREA_WD` | 4 | 地坪、裝飾板 |
| `AREA_WH` | 11 | 牆面、櫃體面材 |
| `LEN_W` | 15 | 收邊條、燈帶、窗簾盒、櫃體 |

該字典用到的單位是 **mm、cm、坪、才、組——沒有 `m2`**。

> **潛伏風險**：舊 `quantity.py` 的 `_area()` 對「才」「坪」換算正確，但 `m2` 直接回傳 cm²（少除 10000）。因為目前字典沒用到 `m2`，此缺陷**不會發作**；一旦有人新增 `m2` 規則就會立刻出現，錯誤量級一萬倍。撈回該檔時務必先修。

## 6　結構體：查詢鍵不同，機制現成

結構體（`00_STR_*`）**完全沒有 `type_id`**——實測 16 個全部無標記。

但 [`qty/README.md`](../../README.md) 早已指出正確路徑：結構體改讀**圖層路徑**查字典。**實測全部命中**：

| 圖層 | type_id | 規則 | 單位 |
|---|---|---|---|
| `00_STR_結構::Beam.樑` | `EX-01` | `VOL_WDH` | m3 |
| `00_STR_結構::Column.柱` | `EX-02` | `VOL_WDH` | m3 |
| `00_STR_結構::Curb.墩座` | `EX-03` | `LEN_W` | cm |
| `00_STR_結構::Slab.樓板` | `EX-04` | `AREA_WD` | 坪 |
| `00_STR_結構::Stair.樓梯` | `EX-05` | `COUNT` | 座 |
| `00_STR_結構::Wall-Brick.牆-Brick` | `EX-06` | `LEN_W` | cm |
| `00_STR_結構::Wall-RC.牆-RC` | `EX-07` | `LEN_W` | cm |

**意義**：結構體不是「計算方式待議」，而是**查詢鍵是 `layer_path` 而非 `type_id`**。字典的 `EX` 類別（7 筆）就是為此準備的。

差異僅在：

- 查詢鍵：`layer_path`（不是 `type_id`）
- 不要求 UUID、空間、高程
- 出現 `VOL_WDH`（裝修材完全沒用到體積規則）
- 加總依 Type，不依房間

## 7　幾何品質

| 檢查 | 結果 |
|---|---|
| 3D Brep 是封閉實體 | **30 / 30** |
| 體積可計算 | **30 / 30**（103 ～ 324,900 cm³） |
| 2D 曲線封閉且共平面 | 61 / 61（雖不採用） |

`COUNT` 類的 7 個 Block：出口指示燈、開關面板、插座面板、瓦斯偵測器、冰箱、浴缸、馬桶——全部是**數量類**，與字典規則完全吻合。

### 7.1 開口

以「毛體積 ÷ 實體積」偵測：

| 物件 | 毛面積 | 淨面積 | 扣除 | 研判 |
|---|---|---|---|---|
| `WL-03` 藝術漆牆面 | 73,840 | 57,592 | **22.4%** | 缺 1.65 m²，尺寸符合一個門洞 |
| `WL-04` 裱布皮革 | 28,808 | 27,360 | 5.0% | 缺 0.14 m²，較像插座開孔 |
| 其餘 24 個 | — | — | **0.0%** | 完美方塊，無開口 |

**扣除率分不出「開口」與「形狀本來就不是方塊」**——`CB-04 鐵件框架` 扣除 95.6%、`CL-03 木作窗簾盒` 79.2%，那些是中空構件不是開口。只能標記後由人判斷。

### 7.2 方位

驗證模型整體繞 Z 軸旋轉 **30°**——這是**刻意的測試設計**，用來確保非 0/90 度的物件不會被算錯。

實測 30 個 Brep 的最佳貼合角度**全部落在 30°**，其中 24 個貼合度正好 1.00（完美方塊，只是轉了角度）。若用 World bbox 量，`WL-14` 會錯到 **127 倍**。

> **實務上牆的方位是各角度混和，而且同一道牆本身就可能有多個角度**（L 型、弧形、包覆）。因此不能靠「整體方位」或「每批一個平面」解決。方法見 [`量測方法.md`](./量測方法.md)。

## 8　MCP 讀取限制（影響實作方式）

worksession 下，MCP 原生工具**看不到 attach 進來的參照物件，且不報錯**：

| 工具 | 對參照物件 |
|---|---|
| `list_objects` | ❌ 看不到（回傳 2972 ＝ 本機數，`truncated: false`、`warning: null`） |
| `get_selection` | ❌ 看不到（即使物件確實選取中） |
| `set_selection` | ✅ 選得到 |
| `run_python` | ✅ 看得到（需正確設定列舉器） |

**「選得進去、讀不出來」的不對稱最危險。**

QTY 要量的 3D 實體全在參照側，因此**所有模型掃描一律走 `run_python`**：

```python
s = Rhino.DocObjects.ObjectEnumeratorSettings()
s.NormalObjects = True
s.LockedObjects = True
s.HiddenObjects = True
s.ReferenceObjects = True      # ← 少了這行就看不到 attach 的物件
s.DeletedObjects = False
objs = doc.Objects.GetObjectList(s)
```

讀取選取狀態要逐一檢查 `o.IsSelected(False) > 0`；`doc.Objects.GetSelectedObjects()` 同樣會漏。

**LoopFlow 2.0 本身沒有這個問題**——`v2/src/loopflow/platform/rhino/live.py:1032` 已明確設定 `ReferenceObjects`，需要排除時還會再檢查一次 `IsReference`。盲點只存在於 MCP 自己的原生工具。qty 應沿用 `live.py` 的既有寫法。

## 9　專案設定與 Registry（2026-09-02 補齊）

前期盤點時 MCP 讀不到 `_LoopFlow_Config`（當時檔案在 2.0.6 根層，或尚未出現在此測檔）。2026-09-02 公司電腦確認後，依 2.0.7 契約搬進 `loopflow/`，並預留 `loopflow_QTY/`。**工作檔不進 Git。**

```text
<_LOOPFLOW_QTY_MCP_WORKFILES_ROOT>/
  _LoopFlow_Config/
    loopflow/
      LoopFlow_Project.json          # schema loopflow.project／1；project_id＝layer_prefix＝M3D
      M3D/
        Project_Registry.json        # schema loopflow.registry／1；registry_revision＝4
        Project_Registry.last-good.json
    loopflow_QTY/                    # QTY 預留；選取 JSON 日後寫這裡，不要寫 AppData
```

| 檢查 | 結果 |
|---|---|
| Registry `objects[]` | **37** |
| 與模型 `_07_UUID` 對得上 | **37 / 37**（只 live、只 Registry 皆 0） |
| `spaces[]` | **3**（`EXT` ＋兩個空間 UUID，對應物件上的廊道／衛浴） |
| `types[]` | **92**（與字典 Type 列數一致） |
| 結構體 | 不在 `objects[]`（2.0 契約如此；仍用 `layer_path` 查字典） |

QTY 選取／一式狀態應寫 `_LoopFlow_Config/loopflow_QTY/`，不得寫進 `loopflow/` 或 AppData。

## 10　尚未完整驗證

| 項目 | 狀況 |
|---|---|
| Registry 與現況的**漂移** | 檔案已在，且目前與模型 UUID **一致**。尚未實測「改字典或改物件後未再發布」時 QTY 要停還是降級（見 [`決策紀錄_1.md`](./決策紀錄_1.md) QO-13） |
| 空間資料的完整性 | 該模型只有「廊道」「衛浴」兩個空間加 `EXT`，不足以驗證分組與排序 |
| 大量物件的效能 | 掃描涵蓋 3116 筆但未計時 |
