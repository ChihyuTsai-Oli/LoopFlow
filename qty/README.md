# LoopFlow 數量計算（qty）

同 LoopFlow repo、**不同產品**。不是出圖 2.0 的模組，也不進 Package Manager `loopflow`。

## 邊界

| 做 | 不做 |
|---|---|
| 單向讀 2.0 已發布的 Registry／物件資料；**結構體**改讀 `.3dm` 幾何 + 圖層 `lf_type_id`／Dictionary `layer_path` + `Q_05`，不要求 UUID／空間／高程 | 寫回物件 UserText、Registry、Nexus |
| 輸出獨立 Excel 估價單（與明細） | 打進 2.0 `.yak` 或工具列 |
| 依 Dictionary 的 `Q_01`～`Q_06` 規則求值 | 把「一式」寫進字典 |

2.0 算出圖；數量計算讀它留下的資料，自己產出 xlsx。`00_STR` 結構體不進 Registry `objects[]`，qty 日後按 Type（必要時再對 FFL 框估樓層）加總，不依房間。契約語意以 `../v2/docs/資料契約.md` 為準。

## 現況

**前期評估階段，還沒有計算程式。** 2026-09-01 已在實際 worksession 模型上完成資料盤點與量測方法驗證。2026-09-02 測檔已補 `_LoopFlow_Config`（2.0.7：`loopflow/` 含 Registry revision 4，並預留空的 `loopflow_QTY/`）；細節見 [`docs/前期評估/資料盤點.md`](./docs/前期評估/資料盤點.md) §9。

### `docs/前期評估/` — 產品面（先讀這裡）

| 文件 | 內容 |
|---|---|
| [`README.md`](./docs/前期評估/README.md) | 索引與閱讀順序 |
| [`範圍與邊界.md`](./docs/前期評估/範圍與邊界.md) | 做什麼、不做什麼、與 2.0 的分界 |
| [`資料盤點.md`](./docs/前期評估/資料盤點.md) | 模型與字典**實際**提供什麼（全部實測數字） |
| [`量測方法.md`](./docs/前期評估/量測方法.md) | 各規則的量測方式與驗證 |
| [`決策紀錄_1.md`](./docs/前期評估/決策紀錄_1.md) | 第一輪：已定案、已推翻、未定案 ＋ 三家覆核 |
| [`決策紀錄_2.md`](./docs/前期評估/決策紀錄_2.md) | 第二輪阻擋項與收斂提案（三種建議強度）；三家已填畢 |
| [`測試模型.md`](./docs/前期評估/測試模型.md) | 驗證模型缺少或可補充的項目 |
| [`開發順序計畫.md`](./docs/前期評估/開發順序計畫.md) | 任務切分、階段順序與雙機停點 |

### `docs/MCP評估/` — 工具面

| 文件 | 內容 |
|---|---|
| `MCP設定交接.md` | Rhino MCP 安裝程序與陷阱（換機時照這份做） |
| `實測報告.md` | MCP 與 worksession 的完整實測記錄 |
| `初步功能分類.md` | GH 元件與 Rhino 工具列的功能分類 |
| `初步評估報告.md`／`數量計算需求筆記.md` | 8/24 的早期文件，已加上實測修正 |

上方「邊界」表中結構體那條**已實測確認**：7 個 `00_STR_*` 圖層全部能以 `layer_path` 查到字典的 `EX-01`～`EX-07`，取得規則與單位。

下一步才寫程式；不要把計算邏輯塞進 `v2/src/loopflow`。
