# LoopFlow 2.0 — 實作總覽

本文件是 2.0 實作範圍、架構邊界與完成關卡的唯一來源。詳細資料欄位見 `資料契約.md`，使用者操作順序見 `工作流程.md`，任務拆分與即時狀態分別見 `開發任務與路徑.md`、`重構進度.md`。

## 目前狀態

- 目標版本：`v2.0.0`
- Runtime：Rhino 8／CPython 3.9／Windows
- 開發模式：在 `wip/` 乾淨重建；不逐支包裝 1.x 程式
- 目前階段：**從開案到 Document 的主鏈已接通。** Extract **公司 Rhino 8 已測（2026-08-20）**。Duplicate Layout／Sync Worksession **家中 Rhino 8 已測（2026-08-20）**。Document **家中 Rhino 8 已測（2026-08-21）**。G02 安裝格式已鎖定為 `.yak`；正在 `codex/v2-g02-yak-spike` 做最小套件（只登錄 `LFDocument`）。TAG-O 只檢查與上色，不實作 Repair。Cabinet／三支 2D 工具不屬 2.0，日後獨立 repo `Loopflow_Toolkit`。
- 正式程式碼：骨架、foundation、Rhino 狀態還原、Dictionary、Nexus 開案檢查至發布、Data Viewer、Tagger Grab、Tagger Laser、Tagger Index、Tagger Layout ID、View Registration、圖目錄 `LF_Catalog`、Infuser Part／All、TAG-O、Extract CP、Duplicate Layout、Sync Worksession、Document；尺寸／數量不屬 2.0
- 介面語言：正式發布只出**一份** `.yak`，內含 tw／en 兩套顯示句子；程式仍認同一組英文 machine key。開發期只維護繁中，現在不抽語系表、不做切換。日後第一次使用問一次繁中／English，記住在這台電腦（不寫進 `.3dm`）。之後改語系用 **Document 右鍵**，不另加工具列按鈕，也不放進 Nexus。Rhino 不猜語系。公開說明維持一篇一語、繁中為真相；入口頁可中英並列。**Tag 圖塊庫例外**：2.0 只維護一份英文 `Tag_Blocks.3dm`。Dictionary 字型：繁中微軟正黑體、英文 Arial。細節見 `系統設定.md`「介面語系」。
- 1.x 定位：工作邏輯與回歸測試的證據，不是 2.0 實作限制。1.x 與 2.0 不可混用。

## 實作文件閱讀順序

1. `實作總覽.md`
2. `資料契約.md`
3. `工作流程.md`
4. `開發任務與路徑.md`
5. `系統設定.md`
6. `重構進度.md`

`rhino指令.md` 記錄開發期目前可跑的 Rhino 指令與按鈕巨集，隨進度同步；它是按鈕現況的單一來源，不取代上列六份規格。G02 正式無底線指令名稱對照亦在該檔與 `指令名稱.txt`。

`前期規劃/` 保存決策過程、1.x 盤點與原始模擬，不作為實作規格。若前期文件與根目錄文件不同，以根目錄文件為準。

## 產品工作鏈

LoopFlow 的核心工作邏輯是：

```text
Type Catalog／Dictionary
→ 建立 3D 模型
→ 建立 Space Boundary
→ 掃描、注入並驗證模型資料
→ 發布 Registry revision
→ 建立 Section／View
→ 產生可獨立編輯的 Drawing
→ 建立 Sheet／圖框
→ 建立並綁定 Tag
→ 同步顯示資料
→ Health 檢查（TAG-O 上色，使用者自行修改）
```

使用者決定何時執行、處理哪些問題、採用哪次更新，以及是否覆蓋可重建成果。系統不得把這條半自動工作鏈改成不可中斷的一次性操作。

## 不可破壞的架構原則

1. Dictionary 是 Type Catalog；3D Object 保存 instance 現值。
2. Layer 是人類分類入口，不是永久資料 ID。
3. Registry 是版本化唯讀發布快照，不是另一份人工資料庫。
4. Section 產生的 Drawing 可獨立編輯；更新不得靜默覆寫人工成果。
5. Sheet metadata 是圖框與輸出命名的真相；Layout 名稱只是輸出。
6. 一般 Tag 綁定穩定 Object／View／Sheet ID；位置只協助找候選。
7. 狀態、revision 與問題是正式資料；顏色只作可復原提示。
8. 每個階段都可預覽、單獨執行、驗證、重跑、取消與復原。
9. 每個產生幾何或改寫資料的指令都有冪等政策，且能辨認前次產出。
10. ID 變更前先列影響、建立 mapping 並可 rollback；不得靜默換號。
11. 模型單位與工程估算單位分離。2.0 只警告文件單位；數量量綱留在 Dictionary 給後續 GH。

## 正式資料實體

| 實體 | 穩定身分 | 唯一責任 |
|---|---|---|
| Project | `project_id` | 工作檔根目錄、schema、發布與 revision 範圍 |
| Type | `type_id` | Dictionary 預設、顯示資訊、估算規則與 layer mapping |
| Model Object | `object_id` | 3D instance 現值、空間、高程與 revision |
| Space | `space_id` | 空間顯示名稱、由樓層框配對的 `level_id` 與 boundary |
| View | `view_id` | Section／平面方向、範圍與固定 2D↔3D transform |
| Drawing | `drawing_id` | 可編輯 2D 成果、來源 revision、provenance 與人工狀態 |
| Sheet | `sheet_id` | 圖種、樓層、序號、圖名、比例與版次 |
| Tag | `tag_id` | Template、binding、人工鎖定、同步 revision 與 health |
| Registry Revision | `registry_revision` | 經驗證後發布的唯讀模型快照 |
| Health Issue | `issue_id` | 問題原因與影響（TAG-O 檢查結果；不自動修復） |

## 模組邊界

```text
wip/src/
  loopflow/
    bootstrap.py
    command_catalog.py  # 指令身分與實作狀態
    runners.py          # 指令 ID → live runner 查表
    features/
      project/        # NX-01 Console 開案檢查
      dictionary/     # C01 Type Catalog 載入／驗證；NX-02 layer 同步
      model_data/     # NX-03 Space；NX-04 物件 ID；NX-05 命中與高程
      registry/
      viewer/         # 只讀資料檢視；C04
      tagger/         # Grab／Laser／Index／Layout ID；D01～D04
      infuser/        # Infuser Part／All；D06／D07
      view/           # 登記 View；E01
      drawing/        # Extract CP／Drawing Materialize；E02
      sheet/          # Sheet metadata API；D04 寫、Catalog／Tag 只讀
      catalog/        # 圖目錄；E05
      health/         # TAG-O；D05
      worksession/    # Sync Worksession；E04
      document/       # 開啟 docs/README.md 文件入口；E06
      drawing2d/
    platform/
      rhino/
      excel.py        # C01 簡單 xlsx，不依賴 openpyxl
      files.py
    foundation/
      config.py
      paths.py
      logging.py
      results.py
      atomic_io.py
      version.py
  entrypoints/
wip/tests/
wip/fixtures/
```

- `entrypoints/` 只轉交 command，不保存第二份業務邏輯。
- `features/` 依一起變動的功能群組織，不按舊按鈕一檔一模組硬切。
- `platform/` 隔離 Rhino、Excel 與檔案系統副作用。
- `foundation/` 只放跨 feature 的小型共用能力。
- `LF_Nexus` 是 Project Console，只擁有開案檢查、同步 Type Layers、高程／空間框、寫入與檢核 Metadata 六個步驟；不再同時承擔 UI、幾何、Excel、Registry 與所有規則。匯出字典、發布與開啟字典是獨立指令，共用 `run_open_check` 與同一套 feature。指令 ID 與 runner 的對應表在 `loopflow/runners.py`，轉交只查表。C02 的工作包見 `Nexus拆分計畫.md`。

## 正式範圍

### 2.0 核心

- Project／path／設定驗證
- Dictionary reader、Type Catalog、layer sync 與差異匯出
- Object ID、Space、高程、資料化 Scan／Apply／Verify
- Registry 安全發布與 Data Viewer
- View Registration、Drawing Materialize、Layout Duplicate
- Grab／Laser／Index／Layout ID、圖目錄 `LF_Catalog`、Infuser、Health（TAG-O 檢查與上色）
- Worksession lifecycle（`LF_Sync_Worksession`）
- Document（`LF_Document`，開啟 `docs/README.md` 文件入口）
- 完整安裝套件與 RC 驗收。G01 只檢查上傳用 3dm 範例，不是使用者升級工具

### 不屬於 LoopFlow 2.0

- `LF_Cabinet_Suite` 與 BOM
- `LF_2D_Cabinet_Gen`、`LF_2D_Shelf_Gap`、`LF_2D_DW_Gen`
- `_CB.01`～`_CB.04` 製作欄位
- Grasshopper Quantity 工作流
- 物件級 Geometry History

上列 Cabinet／三支 2D 工具日後另立獨立 repo **`Loopflow_Toolkit`**（與 R2B／R2O 同級）。不進 LoopFlow 2.0 完成條件、不進 2.0 安裝包、不在本 repo `wip/` 重構。1.x 按鈕可繼續用。字典裡的 `04_CB_櫃體` 仍是一般材質 Type 群組，與 Toolkit 無關。本批不建立該 repo。

## 安全基礎

### Registry

- 使用真正的 exclusive lock，包含 PID、主機與時間。
- 取得 lock 後重新讀取，不以先讀後鎖覆寫別人更新。
- 寫入 pending，flush 並完整驗證後再 atomic replace。
- 發布失敗時保留 last-good，不先刪除正式檔。

### Rhino 狀態

- selection、lock、visibility、color 與 modified state 由 platform adapter snapshot／restore。
- 成功、取消、失敗與 exception 都必須驗證復原結果。
- TAG-O 會把過期 Tag 塗橘寫 `!`、斷連塗紅寫 `?`；再 Infuser 或再綁定後回到 ByLayer。使用者依此自行修改。2.0 **不實作 Repair**。

### Migration

- **不提供** 1.x 做到一半轉 2.0 的產品路徑。1.x 與 2.0 架構與功能都不同，**不可混用**（同一檔、同一工具列、同一套按鈕都不要混）。
- G01 只留作內部工具：最後整理要上傳的 3dm 範例檔時，檢查該檔是乾淨 2.0 資料。
- 日常指令不雙讀、雙寫或偷偷改名。

## 實作關卡

1. **契約關卡**：canonical schema、Block manifest、fixtures、validator 與 migration mapping 完成。
2. **最小架構關卡**：bootstrap、command catalog、foundation、Rhino adapter 與測試骨架可載入。
3. **核心資料鏈關卡**：Dictionary → Object → Registry 可以安全完成並重跑。
4. **圖面與 Tag 關卡**：View → Sheet → Tag → Infuser → TAG-O → Extract CP → Duplicate Layout → Sync Worksession → Document 已接通並實機。TAG-O 只檢查與上色，不實作 Repair。
5. **實機關卡**：Rhino 8 測試正常、取消、失敗、中斷、重跑與人工成果保護。
6. **發布關卡**：`.yak` 安裝套件、RC、公開頁（含 1.x／2.0 不可混用）通過後才合入 `main`。範例 3dm 上傳前才用 G01 檢查該檔。

## 每批完成條件

- 一批只處理一個安全問題或一條 feature。
- contract／fixture／自動測試與適用的 Rhino 實機測試通過。
- 沒有非預期 Rhino 狀態、資料或 last-good 損失。
- 文件、task status、驗證結果與限制同步。
- Git diff 不含秘密、快取、私人工作檔或無關變更。
- commit、push、回復點與未驗證項目可追溯。
