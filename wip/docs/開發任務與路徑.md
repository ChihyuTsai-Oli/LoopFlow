# LoopFlow 2.0 — 任務切分與開發路徑

本文件是任務 ID、工程前置、工作波次與雙機安全停點的唯一來源，把 2.0 重構拆成可在單一工作時段完成、驗證、提交與推送的單位。資料規格以 `資料契約.md` 為準；使用者操作順序以 `工作流程.md` 為準；即時狀態只記錄於 `重構進度.md`。本表只安排工程工作，不另立資料或業務答案。

## 執行規則

- 一次只修改一個 repo；同一 repo 同一時間只由一台電腦／一個 AI 作業。
- 每項任務從 `v2-development` 建立 `codex/v2-<scope>` 短期分支，不直接在整合分支施工。
- 任務開始前確認 upstream、乾淨工作樹與 `pull --ff-only`；結束前完成檢查、commit、push 與交接。
- 任務須小到能留下可驗證的完整狀態。若超過一個工作時段，先拆成子任務，不以未提交檔案換機。
- `%APPDATA%` 只用於正式安裝／RC 隔離驗證；開發期 Rhino 按鈕直接指向 repo 的 `wip/src/entrypoints/`。
- 下表是開發路徑，可隨功能增減、實測結果與架構調整；改動時同步更新本文件、系統設定與 `重構進度.md`。

## 階段與任務

| ID | 任務／建議分支 scope | 前置 | 主要產出 | 完成檢查與安全停點 |
|---|---|---|---|---|
| LF-A01 | 現況工作流與依賴盤點／`workflow-inventory` | 無 | **已完成並封存**；結論已整合進 `實作總覽.md`、`資料契約.md`、`工作流程.md` | 實作不依賴封存草案；需要追查 1.x 證據時才讀 `前期規劃/` |
| LF-A02 | Dictionary 欄位盤點／`dictionary-inventory` | A01 | **已完成**；15 欄、92 Type、measurement rule 92/92 與欄位角色已寫入 `資料契約.md` | XLSX 現況、單位量綱與移除 `_CB.*` 可重現驗證 |
| LF-A03 | 命名與識別契約／`naming-contract` | A01–A02 | **已完成**；`資料契約.md` 已鎖定 serialized machine key、UUID、layer path、Space／View／Drawing／Sheet／Tag 身分、指令與檔名 | 名稱與 enum 帶 `schema_version`；正式資料未改 |
| LF-A04 | Registry／Tag／圖框資料契約／`registry-tag-contract` | A01–A03 | **已完成**；Registry 根／types／spaces／objects 欄位與 10 份 Block manifest 已寫入 `資料契約.md` 與 `wip/fixtures/schema/` | 24 個 legacy key 與畫面擷取一致；`TAG_DW` 全手動、家具 `block_name`、`title_frame` 才寫圖號、未知 Block 零寫入 |
| LF-A05 | Nexus 專用拆分計畫／`nexus-plan` | A01–A04 | **已完成**；`Nexus拆分計畫.md` 列出 NX-01～07 工作包、與 C01／C03／C05 的界線，以及每包 fixtures／驗收 | 本任務只規劃，未重寫 Nexus；C02 依該文逐項開分支 |
| LF-A06 | Fixtures 與 golden baseline／`contract-fixtures` | A02–A05 | **已完成**；`wip/fixtures/contract/` 與 `wip/tools/check_contract_fixtures.py` 可驗證 Dictionary／UUID／Space／frame／Registry／Drawing／Tag | 92/92 量綱、15 欄、24 個 Block key、EXT 四因、Duplicate 清除／保留與 Drawing 零／一／多來源已通過；不含私人正式專案 |
| LF-B01 | 最小 source 與測試骨架／`source-skeleton` | A03–A06 | **已完成**；`wip/src/loopflow/`、`entrypoints/LF_Nexus.py` 與 `wip/tests/test_import_smoke.py` | 套件可 import；Nexus 入口載入後回報 `not_implemented`，不假裝 Scan／Apply 完成；未建 feature 模組 |
| LF-B02 | Result／Logging／Version／Config／Path／`foundation-core` | B01 | **已完成**；`wip/src/loopflow/foundation/` 提供 Result、schema 檢查、進階設定，以及以 `.3dm` 所在資料夾為根的路徑解析與 `LoopFlow_Project.json` 專案設定 | `python wip/tests/test_foundation.py` 通過；未存檔即停止且不建檔；原始碼無個人路徑；未實作 Rhino adapter |
| LF-B03 | Rhino platform 與狀態復原／`rhino-platform` | B01–B02 | **已完成**；`platform/rhino` 提供 snapshot／restore 與 `run_guarded`；測試用 `MemorySession` | 成功／取消／失敗／例外路徑有純 Python 測試；live adapter 可載入但 **未在 Rhino 8 實機驗證** |
| LF-C01 | Dictionary reader／validator／`dictionary-core` | A06、B02 | **已完成**；`features/dictionary` 載入 15 欄、拆 `type_id`、量綱檢查；未知版本停止；xlsx 以標準庫讀取 | `python wip/tests/test_dictionary.py` 通過；A06 15 欄／92 列／cases 與版本拒絕已覆蓋；未實作 Nexus |
| LF-C02 | Nexus 重建工作軌／依 `Nexus拆分計畫.md` 的 NX-01～07 逐項開分支 | A05–A06、B02–B03、C01 | **已完成**：NX-01～05、檢核 Metadata、匯出字典、NX-07 發布交接；NX-06 取消（2.0 不算尺寸） | 選單 5 寫入模型 Metadata／6 檢核模型 Metadata；匯出 Type Layers 為字典與發布串接資料改為獨立指令。**不得處理 `_CB.*`** |
| LF-C03 | Registry 安全發布 P0／`registry-publisher` | A04、B02、C02 所需 payload | **已完成**：exclusive lock（PID／主機／時間）、pending、validate、atomic replace、last-good；NX-07 已接獨立指令 `LF_Publish_Exchange`。擋下時彈窗與 Nexus 6 同一份清單；檔案佔用時短重試 | `python wip/tests/test_registry_publisher.py` 通過；家中 Rhino 8 已測不合規不能發布、成功有正式檔與 last-good；佔用重試尚未單獨實機 |
| LF-C04 | Data Viewer／`data-viewer` | A03–A04、B03、C01 | **已完成**；`LF_Data_Viewer` 只讀顯示 canonical 欄、缺值、舊 key 來源與殘留尺寸警告 | `python wip/tests/test_data_viewer.py` 通過；不寫入；未知 schema 停止；家中 Rhino 8 已測 |
| LF-C05 | Dimension／Quantity／`dimension-quantity` | — | **不屬 2.0**。數量欄 `Q_01`～`Q_06` 留在 Dictionary；換算與幾何求值交給後續 GH | 2.0 不實作、不接 Nexus |
| LF-D01 | Tagger Grab／`tagger-grab` | A04、B03、C01–C03 | **已完成**；`LF_Tagger_Grab` 在 Layout 選 Tag、點進 Detail 再選來源；寫 `lf_source_object_id` 或家具 `lf_source_block_name`；Block 名不分大小寫。鎖定含 `lf_00_lock_state` 與舊欄 `x`／`X` | `python wip/tests/test_tagger_grab.py` 通過；家中 Rhino 8 已測 Layout→Detail 選 2D 線；舊鎖定欄 `x`／`X` 家中已測（2026-08-18） |
| LF-D02 | Tagger Laser／`tagger-laser` | D01 共用契約、E01 固定 View transform；E02 來源索引可用時整合 | **已完成**；`LF_Tagger_Laser` 在 Layout 選 Tag，在 Detail 內點一下，用框內剖面 Hatch／Curve（不含 Visible 背景）與登記時寫死的 transform、沿 CP 法線射線寫 `lf_source_object_id`；沒有框時請先執行 Anchor Frame；多候選清單只顯示圖層名；日常不畫測試線，**選 Tag 時**命令列可開 `DebugRay`；本批不接 E02 來源索引 | `python wip/tests/test_tagger_laser.py` 通過；家中 Rhino 8 已測綁定。Esc／Detail 外／鎖定／Grab 圖塊／0 或 ≥2 個 View／沒打到／無 UUID 零寫入。舊鎖定欄 `x`／`X` 家中已測（2026-08-18）。**公司對位與 DebugRay 已測（2026-08-20）** |
| LF-D03 | Tagger Index／`tagger-index` | D01 共用契約 | **已完成**；`LF_Tagger_Index` 在 Layout 選 Index Tag，從可搜尋清單選全檔 Layout Detail（點選時 zoom），唯一對到已登記 View 才寫 `lf_target_view_id` 與所選頁名 `lf_target_layout`；不寫圖號顯示欄、不發明 `sheet_id`；`TAG_ELEV_0` 拒絕 | `python wip/tests/test_tagger_index.py` 通過；家中 Rhino 8 已測清單＋zoom＋綁定。Esc／鎖定／Grab／Laser／圖框／非 Layout／無 Detail／0 或 ≥2 個 View／取消清單零寫入。舊鎖定欄 `x`／`X` 家中已測（2026-08-18） |
| LF-D04 | Tagger Layout ID／`tagger-layout-id` | D01、Layout 契約 | **已完成**；`LF_Tagger_Layout_ID` 依頁序寫 Sheet metadata 與圖框圖號／圖名。多頁共用同一 `lf_sheet_id` 時第一頁沿用、其餘發新 id | 頁名 `**圖類別__圖號__圖名`，寫入後起點頁保留 `**`；`//` 手動頁不編號但寫圖框；圖號尾端為數字即放行；缺 `**` 停止且彈窗只顯示說明；canonical `lf_drawing_no`／`lf_drawing_name`。D08 後不寫舊欄 `DWG_NO`／`DWG_NAME`。家中 Rhino 8 已測（2026-08-18） |
| LF-D05 | TAG-O／`tag-o` | D06–D07 Infuser 已可跑 | **已完成**；`LF_TAG-O` 開可捲動色碼面板，依 Layout 頁序列出已綁定 Tag（頁間灰線）；`[正常]` 綠、`[過期]` 橘寫 `!` 塗橘、`[斷連]` 紅寫 `?` 塗紅；未綁定不列出；刪目標 Layout／Detail 可偵測；鎖定不改外觀；再綁定後 Infuser 可恢復；**只檢查與上色，不實作 Repair** | `python wip/tests/test_tag_o.py` 通過；**已合入 `v2-development`（`4efd3c5`）**；**家中 Rhino 8 已測（2026-08-19）** |
| LF-D06 | Infuser Part／`infuser-part` | D01–D04、D08、C03 | **已完成**；`LF_Infuser_Part` 當頁注入 D08 Tag 的 `lf_*` 顯示欄；非 D08 略過；家具跟綁定實例現況名稱；斷連寫 `?` 不灌回，再綁定後可恢復；鎖定／`TAG_DW`／圖框／`TAG_ELEV_0`／manual 不覆寫 | `python wip/tests/test_infuser_part.py` 通過；**公司 Rhino 8 已測注入（2026-08-19）**；**家中全鏈含斷連／過期外觀（2026-08-19）** |
| LF-D07 | Infuser All／`infuser-all` | D06 | **已完成**；`LF_Infuser_All` 全檔 Layout 頁注入，規則與 Part 相同；模型空間也可跑 | `python wip/tests/test_infuser_all.py` 通過；**公司 Rhino 8 已測（2026-08-19）** |
| LF-D08 | Tag Block 顯示欄位資產更新／`block-asset-fields` | D04；**須早於 D06** 才看得到 Infuser 結果 | **已完成**；公司 Rhino 圖塊已改（2026-08-19）；Layout ID 已停雙寫。`fixtures/legacy/tag_block_text/` 保留 1.x 證據不覆寫 | 圖框讀 `lf_drawing_no`／`lf_drawing_name`／`lf_scale`；Tag 讀 `lf_*`；鎖定讀 `lf_00_lock_state`；2.0 只維護一份英文圖塊庫。未輸入過 `x` 的實例列表可不顯示鎖定欄，改插新圖塊 |
| LF-E01 | View Registration／Anchor migration／`view-registration` | B03、A03 | **已完成**；`LF_Anchor_Frame` 在 2D 模型空間框選剖面物件＋Text Dot，**彈出視窗**輸入外擴距離，寫 `lf_view_id`、Clipping Plane 物件 ID 與固定 transform；1.x 框可升級不刪線稿。可對圖 A 或 Extract 後的圖 B 執行 | `python wip/tests/test_view_registration.py` 通過；家中 Rhino 8 已測平面／天花／立面。不以名稱／bbox 猜測；0 或 ≥2 個 CP 即停 |
| LF-E02 | Extract CP／Drawing Materialize／`extract-cp` | E01、C02–C03 的 Object ID／revision | **已完成**；`LF_Extract_CP` 勾選剖面根圖層，複製到 `LoopFlow_Extract::Visible`／`Hatch`／`Curve_#RRGGBB`（可列印；Visible／Hatch 列印色 `#BEBEBE`，其餘黑）；清掉 3D UserText，寫 `drawing_id`、來源 `view_id`、revision 與 `source_object_ids[]`；重跑選取代／新增／略過；modified 不覆蓋 | `python wip/tests/test_extract_cp.py` 通過；**公司 Rhino 8 已測（2026-08-20）** |
| LF-E03 | Duplicate Layout／`duplicate-layout` | E01、D04、A04 | **已完成**；`LF_Duplicate_Layout` 加高清單可複選來源 Layout、彈窗輸入一份份數套用所有選頁，以 Rhino API 複製（不碰剪貼簿）；新 `sheet_id`／`drawing_id`／`tag_id`／`catalog_id`（含 Layout ID 登錄的自訂圖框）；除 `TAG_DW` 外清除 binding，手填欄寫空白，自動欄改斷連 `?` 並塗紅，**不改 lock**；`TAG_DW` 全內容保留；圖框圖號／圖名先清、比例保留 | `python wip/tests/test_duplicate_layout.py` 通過；**家中 Rhino 8 已測（2026-08-20）** |
| LF-E04 | Sync Worksession／`worksession` | B02–B03 | **已完成**；`LF_Sync_Worksession` 第一次開始監看目前檔案同資料夾的 `.3dm`，Rhino idle 且延遲 0.5 秒後 Refresh Worksession；再跑一次停止；資料夾變了則改監看新位置。只 Refresh，不 Attach／Detach。失敗延遲再試，不拆上一份參照 | `python wip/tests/test_worksession.py` 通過；**家中 Rhino 8 已測（2026-08-20）** |
| LF-E05 | LF_Catalog 圖目錄／`catalog` | D04 的 Sheet metadata API | **已完成**；`LF_Catalog` 只消費 Sheet metadata，定位點綁 `sheet_id`。面板六顆鈕：選取圖號／圖名／Layout、生成 圖號/圖名、清除、匯出。無 Refresh、無關閉鈕；生成／清除／匯出成功後關面板 | `python wip/tests/test_catalog.py` 與契約檢查已通過。家中 Rhino 8 已測（2026-08-18／22）：列印寬度 `No Print`、定位點歸位、生成、文字外觀記在定位點。只消費 Sheet metadata；文字在 `LoopFlow::Drawing_Text`；可清除定位點並還原選取前圖層；Esc 與預覽取消零寫入 |
| LF-E06 | Document／`document` | 公開 GitHub 文件頁 | **已完成**；`LF_Document` 只開 `https://github.com/ChihyuTsai-Oli/LoopFlow/blob/main/docs/README.md`（文件入口，不是專案首頁）。頁內中英並列，再連繁中／英文說明。內容之後再改。舊名 `LF_Help` 不登錄 | `python wip/tests/test_document.py` 通過；**家中 Rhino 8 已測（2026-08-21）** |
| LF-F01 | 2D Cabinet／Shelf Gap／DW／Cabinet Suite | — | **不屬 LoopFlow 2.0**。日後獨立 repo `Loopflow_Toolkit`（與 R2B／R2O 同級）。本批不建 repo、不搬 1.x 程式 | 不進 2.0 完成條件與安裝包。1.x 按鈕可繼續用 |
| LF-G01 | 範例檔檢查／`migration` | 核心功能完成；要上傳 3dm 範例時才做 | **不是給 1.x 使用者的升級工具。** 原則上不需要。只在最後整理要上傳的 `.3dm` 範例檔時，掃描／確認該檔是乾淨 2.0 資料。不提供 1.x 做到一半轉 2.0 的產品路徑 | 只對範例副本執行。現在不要開 |
| LF-G02 | Build／Installer／`build-release` | 核心功能與資產結構完成；格式已記入文件 | **格式已鎖定：** 主格式 `.yak`；同一份上 Package Manager 與 GitHub Release（＋checksum）；不做 RHI。1.x ZIP＋bat＋拖 RHC 凍結。套件含程式本體與 LoopFlow 工具列（RUI，不覆蓋 workspace）。命令列登錄無底線正式名稱。語系：一份 yak、兩套顯示句子；改語系用 Document 右鍵。`0.2.2` 含 Catalog 修正；**家中 Rhino 8 已測安裝與正式指令（2026-08-22）** | 正式版不依賴 repo Python 路徑；Section 巨集還在。升級／重複安裝／移除尚未單獨驗收。不含 D08 migrate、Cabinet／2D。不實作右鍵語系、不上架 |
| LF-G03 | 端到端 RC／`rc-validation` | G02 | 隔離 Rhino 8 的完整 workflow 驗收記錄。公開頁面（入口／README／使用說明）須寫：**1.x 與 2.0 架構與功能都不同，不可混用** | 正常、取消、失敗、中斷、來源零變更與 last good 全部驗證 |

## 建議開發波次

1. **契約波次**：A01–A06。只建立可裁決、可測的資料定義，不搬正式功能。
2. **架構波次**：B01–B03。只建立第一條功能真正需要的最小骨架。
3. **核心資料鏈**：C01–C04。Nexus 依 `Nexus拆分計畫.md` 的 NX-01～05、NX-07 逐包實作；C05 尺寸／數量不屬 2.0。
4. **可重複功能接入**：D01–E05 主鏈、TAG-O、Extract CP、Duplicate Layout、Sync Worksession 與 Document 已實機。操作邏輯：Infuser 注入 Tag → TAG-O 確認活著或斷連 → 下一輪 Infuser（斷連須再綁定後才灌回）。TAG-O **只檢查與上色**；**不實作 Repair**。G01 不是使用者升級工具；現在不要開。G02 `0.2.2` 家中已測安裝與正式指令。
5. **Loopflow_Toolkit（日後）**：Cabinet Suite 與三支 2D 工具不在本 repo 重構。獨立 repo 與 R2B／R2O 同級，建 repo 前再排程。
6. **切換與發布**：G02 格式已鎖定為 `.yak`。`0.2.2` 家中已測安裝與正式指令；不要上架。G01 只在最後上傳 3dm 範例檔前做內部檢查。G03 公開頁須寫 1.x 與 2.0 不可混用。完成後才合入 `main`。

## 雙機換機檢查點

每次換機前必須達成：工作樹乾淨、任務分支已 push、upstream 差距 `0/0`、`重構進度.md` 記錄完成／未完成事項與下一步。另一台電腦先拉 workspace root 與全部子 repo，再從遠端分支續作；不可用雲端同步資料夾或手動複製程式碼取代 Git。
