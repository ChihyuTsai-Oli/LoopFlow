# LoopFlow 2.0 — 資料生態決策表

> 寬版閱讀檔：[資料生態決策表.html](資料生態決策表.html)。HTML 由 `wip/tools/build_decision_table_html.py` 依本檔產生，不應手動編輯。

這是 `資料生態藍圖.md` 的使用者編輯區。藍圖保存現況、工作鏈與架構說明；所有尚未定案、需要使用者確認或補充實務語意的事項集中在本文件。原本散落在 Nexus／Dictionary 菜單的 `ND-*` 細項也已併入本檔，避免同一問題有兩套答案。

## 使用方式

- 可以直接修改表格中的「你的決定／說明」，不需要使用固定回覆格式。
- 「AI 建議整合／衝突」整理兩位 AI 的共同結論或做法差異；任一 AI 明確標為「強烈建議」且做法不衝突的安全措施，直接納入「你的決定／說明」。
- 若強烈建議只涵蓋一部分，只採用該部分，其餘實務語意維持待決定；若兩位 AI 的具體做法衝突，只採用無衝突的共同安全底線，衝突部分維持待決定。
- 可以把 `待決定` 改成「採用」、「不採用」，或直接寫自己的規則。
- 可以改寫建議內容；若選項不符合實務，直接刪掉重寫也可以。
- 請保留 `ECO-*`／`ED-*`／`ND-*` 編號，方便後續追蹤與回寫。
- ND 細項若標示「由某 ECO／ED 統一裁決」，只填上方對應列，避免同一問題出現兩個答案。
- AI 讀取你的修改後，先整理歧義與影響，再同步回寫藍圖、命名與資料契約、Nexus／Dictionary 盤點及進度紀錄。
- 在回寫完成前，本文件是尚待確認事項的唯一來源；不要同時在藍圖與本文件維護兩份答案。

### AI 建議強度

本輪自動裁決日期：2026-08-14。依使用者指示，任一 AI 的強烈建議直接採用；若強烈建議只涵蓋部分安全措施或兩位 AI 的具體做法衝突，僅採用無衝突部分，其餘維持待決定。

| 強度       | 意義                                                      |
| -------- | ------------------------------------------------------- |
| **強烈建議** | 最好照做；這通常關係到資料不斷鏈、可復原、避免靜默錯誤或能否長期擴充。若不採用，需要先設計同等安全的替代方案。 |
| **一般建議** | 可以改，但會增加相應的規則、操作或維護成本；決定時需明確接受代價。                       |
| **輕鬆建議** | 原則上不影響核心資料鏈，可依實際習慣、時程或介面偏好調整。                           |

## A. 上位生態原則

兩次獨立複核都支持以下方向；依本輪規則，含強烈建議且無做法衝突者已直接採用。

| ID     | 建議原則                                                 | CodeX 建議（強度）                                    | Claude 建議（強度）                                                                                                                                                      | AI 建議整合／衝突                                               | 你的決定／說明       |
| ------ | ---------------------------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------- | ------------- |
| ECO-01 | Dictionary 是 Type Catalog；3D Object 是 Instance truth | **強烈建議**：採用。避免 Type 預設與 Object 現值互相覆寫、形成兩份真相。   | **強烈建議**：採用。這是「恢復 Dictionary 預設」能成立的前提。現況 `_02`／`_13` 靠「值為空才寫入」模擬繼承，使用者無法分辨某個值是預設還是自己改過的。                                                                          | 無衝突：Dictionary 管 Type 預設，3D Object 保存 Instance 現值。       | **採用**        |
| ECO-02 | Layer 是人類分類入口，不是永久資料 ID                              | **強烈建議**：採用。Layer 改名或重組時，不應讓既有 Tag 與資料關聯一起斷線。   | **強烈建議**：採用。Dictionary 已有 92 個 Type、12 個群組且持續成長，重新分類是遲早的事；現況 layer path 就是主鍵，一改就全斷。                                                                                | 無衝突：Layer 只作分類入口，資料關聯使用穩定 ID。                            | **採用**        |
| ECO-03 | Section 圖面可獨立編輯；更新不得靜默覆寫人工成果                         | **強烈建議**：採用。這是保護出圖人工成果的核心安全線。                   | **強烈建議**：採用。人工描圖是整條鏈上**唯一無法自動重建**的成果，其他階段都能從模型重跑。                                                                                                                  | 無衝突：更新必須保護人工編輯，不得靜默覆寫。                                   | **採用**        |
| ECO-04 | Tag 綁定穩定 Object／View／Sheet ID；圖面位置只協助定位              | **強烈建議**：採用。否則名稱、位置或圖面調整都可能造成無法察覺的錯綁。           | **強烈建議**：採用。這就是產品的核心承諾本身，不是實作細節；若不成立，其餘功能都只是畫圖工具。                                                                                                                  | 無衝突：Tag 以穩定 ID 綁定，位置不得作為身分。                              | **採用**        |
| ECO-05 | Sheet metadata 是圖框與命名真相；Layout 名稱是輸出                 | **強烈建議**：採用。日後插頁、改圖號格式或多種交付格式才不需拆字串回推。          | **一般建議**：方向同意，但這是 A 表唯一**失敗可回復**的一條——現況圖號在 render 當下重新解析，弄亂了重跑 Layout_ID 就會修好，不會遺失資料。建議採用但分階段：先讓 metadata 成為產生 Layout 名稱的來源，完整 Sheet 管理介面延後。代價是初期 metadata 仍需人工維護。 | 無方向衝突；採 Claude 的分階段做法：先由 metadata 產生 Layout 名稱，完整管理介面延後。 | **採用，分階段實作**  |
| ECO-06 | Registry 是版本化唯讀發布快照，不是另一份人工資料庫                       | **強烈建議**：採用。避免 3D 模型與 JSON 可各自編輯後產生衝突。          | **強烈建議**：採用。兩份可各自編輯的同源資料是最典型的不可回復衝突；一旦分岔就無法判斷哪邊才對。                                                                                                                 | 無衝突：Registry 只作版本化唯讀發布快照。                                | **採用**        |
| ECO-07 | 狀態、revision 與問題是正式資料；顏色只作可還原提示                       | **強烈建議**：採用。顏色不能可靠判斷真實狀態，也不能破壞使用者原色。            | **強烈建議**：採用。現況 TAG-O 完全以 RGB 判定，且 Infuser 清除提示時把 ColorSource 設回 ByLayer——這已經是實際的資料破壞，不只是判斷不準。                                                                      | 無衝突：狀態使用正式欄位，顏色僅作可還原提示。                                  | **採用**        |
| ECO-08 | 每階段可單獨執行、驗證、重跑與復原                                    | **強烈建議**：採用。符合雙機、分時工作與小批次測試，也降低一次大操作的風險。        | **強烈建議**：採用。這一條是其他十條**能被測試**的前提；沒有它，任何驗證都得靠整案重跑，實機測試成本會失控。                                                                                                         | 無衝突：每階段都必須可獨立執行、驗證、重跑與復原。                                | **採用**        |
| ECO-09 | 模型單位先驗證；工程估算單位分離；所有量綱常數具名並標註單位                       | **強烈建議**：採用。單位混用會直接產生錯誤尺寸、搜尋距離與數量。              | **強烈建議**：採用，但關鍵在時機。乾淨重建時替常數命名、標單位幾乎零成本；等功能寫完再回頭補，就得重新驗證每一個幾何行為。這是「現在做免費、以後做很貴」的典型。                                                                                 | 無衝突：重建初期即分離單位契約，所有量綱常數具名並標註單位。                           | **採用，列入重建初期** |
| ECO-10 | 每個產生幾何或改寫資料的指令都定義冪等重跑政策，並能辨識前次產出                     | **強烈建議**：採用。避免 Extract、參考線或 Sheet 重跑後無限累積與複製身分。 | **強烈建議**：採用。這是使用者**每天會遇到**的問題（Extract 重跑疊圖、Dict-to-Layer 累積參考線），不是理論風險。                                                                                            | 無衝突：所有產生／改寫指令都要能辨識前次產出並定義重跑政策。                           | **採用**        |
| ECO-11 | ID 的產生與變更可追溯；自動換 ID 前先報告、預覽、建立 mapping 並可回復          | **強烈建議**：採用。這直接防止 UUID 重建造成既有 Tag 不可逆斷鏈。        | **強烈建議**：採用，且建議列為**全部項目中的第一優先**。這是整份複核中唯一會造成不可回復損失的路徑：複製一個已標註物件後跑 TagTrigger，原件與複本的 UUID 會**同時**被換掉，且只回報數量、不留舊值。                                                   | 無衝突：ID 變更前必須預覽、保留 mapping 並可回復；列為第一優先。                   | **採用，第一優先**   |

## B. 證據已足、建議優先確認

這些已有明確程式或 Dictionary 證據；含強烈建議且無做法衝突者已直接採用，仍可由使用者日後翻案。

| ID    | 現況與建議                                                                                                                                                 | CodeX 建議（強度）                                                                  | Claude 建議（強度）                                                                                                                         | AI 建議整合／衝突                                                             | 你的決定／說明                       |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ----------------------------- |
| ED-01 | Dictionary `_03_ID編號` 的 92 筆現值都是「類別碼-序號」，但家具 Block 又以 `FF-01__Chair-1` 形成 Dictionary 外的另一套編碼；2.0 需拆 `type_category`／`type_sequence`，並決定 `FF-01` 的資料身分 | **強烈建議**：Dictionary ID 照此拆分；家具編碼另由 ED-14 裁決，不能直接塞進同一欄假裝同源。                    | **強烈建議**：拆分成本接近零，且可驗證 category 與 layer 群組。新增證據顯示 `TAG_ITEM` 的同一顯示欄可能來自 Dictionary 或 Block 名稱，兩套詞彙必須在 schema 明確分流。                     | 無衝突：拆分 Dictionary 類別碼／序號，家具編碼另立 schema；家具身分由 ED-14 定案。                | **採用拆分與分流；家具維持獨立 namespace，同款家具可共用名稱與代碼。**        |
| ED-02 | 正式 8 種可鎖 Block 共用 `attr_Lock_不更新>寫入x或X`，現行 Grab／Laser／Index／Infuser 都辨認得到；但規則散落、只接受單一 `x`／`X`，且鎖定同時凍結重新綁定與資料寫入。建議統一為 typed `lock_state`               | **強烈建議**：統一。核心風險是使用者填其他標記時靜默未鎖，以及 locked Tag 不再更新 health，不是正式 Block 目前彼此判斷分歧。 | **強烈建議**：以 UI toggle 產生 boolean／enum；migration 將非 `x/X` 的既有值列為待確認。保留「完全凍結內容與重新綁定」的使用意圖，但 Health 仍唯讀檢查來源。                              | 無衝突：以 UI toggle 寫入 canonical boolean／enum；migration 處理舊值，Health 仍唯讀檢查。 | **採用**                        |
| ED-03 | Cabinet 現有程式已持有 panel 的 true W／H／D，卻在寫入前排序抹除方向；建議 L／W／T 依 panel local frame                                                                           | **強烈建議**：採 local frame。資訊已存在，不需要再靠三邊大小猜方向。                                    | **一般建議**：技術結論同意，但優先序不同。`_CB.*` 四欄目前**沒有任何 consumer**，方向錯了今天也不會有人看到。真正該固定的要求是「Cabinet 重建時不要再 `sorted()`」，而不是現在就得動工；驗收條件等 BOM 消費端定義後再補。 | 無技術衝突：採 local frame、不再排序抹除方向；Claude 建議延後實作。                            | **不再適用：`_CB.01`～`_CB.04` 四欄已於 2026-08-14 從字典移除，2.0 沒有這組欄位可定義方向。若未來 Cabinet 成為獨立建模工具／外掛，再於該專案採 local frame 原則重新定義。** |
| ED-04 | Rhino 模型文件單位與 Dictionary `_08_單位` 是兩件事；建議拆成模型單位契約與工程估算單位契約                                                                                            | **強烈建議**：拆分。兩者用途與量綱完全不同，合併只會製造錯誤推導。                                           | **強烈建議**：拆分。這是定義層面的修正，成本為零，卻能擋掉一整類錯誤推導——例如把 `坪`、`組`、`樘` 當成模型單位去做換算。公開指南本身已定義 `_08` 為「數量計算單位，影響工程估算報表」。                                | 無衝突：模型文件單位與工程估算單位分成兩套契約。                                               | **採用**                        |
| ED-05 | Drawing lifecycle 先完成「重跑時辨識前次產出，讓使用者選擇取代／新增／略過」，再建立完整狀態機                                                                                              | **強烈建議**：先完成這個最小安全基礎。沒有冪等性，後續狀態機無法可靠運作。                                       | **強烈建議**：同意。補充：這與其說是功能，不如說是**順序**。先做冪等再做狀態機，成本幾乎為零；反過來做，六種狀態全都得重寫，因為狀態機的每個判斷都預設「能認出前次產出」。                                             | 無衝突：先建立前次產出辨識與取代／新增／略過，再做完整狀態機。                                        | **採用，固定實作順序**                 |

## C. 需要實務語意的問題

這些無法只靠讀碼正確決定，可以直接在最右側「你的決定／說明」欄自由描述實際工作方式。

| ID    | 需要確認                                                                       | 目前觀察／可參考方向                                                                                         | CodeX 建議（強度）                                                                                                                                                               | Claude 建議（強度）                                                                                                                                                                                                               | AI 建議整合／衝突                                               | 你的決定／說明                                                                            |
| ----- | -------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| ED-06 | 高程 `CH`／`BC` 的正式語意                                                         | `CH` 現況顯示 CH、幾何取物件底面；`BC` 對 Block 取插入點，非 Block 卻靜默退回底面                                             | **一般建議**：保留 CH＝天花物件底面、BC＝Block 插入點，但把規則、顯示標籤與前置條件分開；非 Block 使用 BC 時直接報錯。代價是部分舊模型需修正。                                                                                       | **強烈建議**（提高）：請把這題拆兩半看。「CH／BC 各代表什麼」只需你確認一句話，屬輕鬆；但「BC 前置條件不成立時必須報錯」我認為屬強烈。Dictionary 有 16 列使用 BC，若物件不是 block instance，Tag 會**顯示 BC 卻計算 BH**，等於印出錯誤的施工高度且全程無提示——這是會流到現場的錯誤，不只是資料不整潔。                                          | 無衝突的強烈部分：BC 前置條件不成立時必須報錯；CH／BC 正式語意仍需使用者確認。              | **部分採用：BC 非 Block 直接報錯，BC 必須為 Block；CH 是天花板底部高程，取物件底部高度；BC 取 Block 基準點為高程。BH＝物件底部高程、TH＝物件頂部高程。`TH/BH` 原本只用於樓板，字典已改為只填 `TH`，2.0 enum 只保留 BH／TH／CH／BC 四種，`TH/BH` 只由 migration 辨識舊值。** |
| ED-07 | Space boundary 是否會重疊、是否同時有多樓層                                              | 現況取 bbox 底面中心命中的第一條 boundary；需要定 priority／level／衝突行為                                               | **一般建議**：Space 使用穩定 ID、level 與 priority；多重命中時停止並列出衝突。代價是 boundary 需要多幾個正式欄位。                                                                                               | **一般建議**：同意，但建議先做一個更便宜的中間步驟。現況「沒命中任何 boundary」一律回 `EXT`，所以**「這東西在室外」與「這東西沒被任何空間涵蓋」無法區分**，而 TAG-O 的空間覆蓋檢查正好依賴這個值。先把「未命中」與「室外」分成兩種結果，比完整的 priority／level 便宜很多，卻立刻讓覆蓋檢查可信。                                                     | 雙方均無強烈建議；方向相容但實作順序不同。                                    | **採用：未命中維持 `EXT`，表示位於 Space 外；同一 `.3dm` 可有多樓層，每個 Space Boundary 必須帶穩定 `space_id` 與 `level_id`。相鄰邊界可以共邊，但內部面積不可重疊；發現重疊時停止並顯示所有衝突 Space 名稱。已知代價：`EXT` 同時代表「真的在室外」與「漏畫 boundary」，兩者不再區分。補償做法是空間覆蓋率報告直接列出被判為 `EXT` 的物件清單，並標明落入 `EXT` 的四種成因——沒有 boundary 圖層、圖層存在但沒有邊界、取不到 bbox、有邊界但未命中——由使用者目視確認，而不是新增第三種狀態。** |
| ED-08 | 不同 Type 的寬、深、高如何定義                                                         | 現況一般物件偏 world bbox；建議依 Type 與 local frame 定義                                                       | **一般建議**：以 local frame 為預設，再允許各 Type 指定尺寸規則。代價是前期要為主要 Type 建立規則與 fixtures。                                                                                                 | **輕鬆建議**（降低）：`_05`／`_06`／`_07` 目前**沒有任何 consumer**，所以現在定義了也無從驗證對錯，很容易訂完又改。建議把順序反過來：先確定「誰要用這三個欄位」（BOM？估算表？外部工具？），再依那個 consumer 的實際需求定義方向規則。在那之前保留欄位並明確標為「未定義」即可。                                                             | 雙方均無強烈建議；對定義時機有差異。                                       | **採用：`_05`／`_06`／`_07` 在 Dictionary row 保持空白，由模型計算後寫入物件。尺寸使用物件／Block local frame。**軸向固定，不比大小**：高度取 local 垂直方向、寬取 local X、深取 local Y。先前「較大值為寬、較小值為深」的動態判定已否決，因為它會讓薄長物件（厚牆、窄柱、踢腳）的寬深對調，使 ED-09 的面積計量規則算錯而且無人察覺。代價是薄長物件的「寬」可能小於「深」，看起來反直覺，但語意穩定且可驗證。** |
| ED-09 | `_09_實作數量` 是否要實作                                                           | 目前沒有 producer；若要實作，需定義每種 `_08_單位` 對應的長度／面積／體積／計數規則                                                 | **輕鬆建議**：2.0 第一階段保留 schema，但先設為人工／外部值；等估算需求明確再逐種單位實作。主要代價只是暫時沒有自動數量。                                                                                                       | **輕鬆建議**：同意設為人工／外部值。繁中與英文 Dictionary 指南已先修正為「現行 1.x 沒有自動 producer」，所以這裡只剩是否要在 2.0 新增估算功能的產品決定。                                                                                                                              | 雙方均無強烈建議；共同方向是先作人工／外部值。                                  | **採用自動計算：由 ED-08 的 local W／D／H 與 Type 計量規則產生 `_09`。`坪 = 指定面積（m²）× 0.3025`；`才 = 指定面積（cm²）÷ (30.3 × 30.3)`，即 1 才 = 918.09 cm²（採台尺 30.3 cm，非簡化的 30 cm）；`樘`、`片`、`組`、`台`、`座` 等計數單位每個物件計 1，再於報表加總。體積使用 W × D × H 並換算至 `_08` 指定單位。`cm` 與 `mm` 都是長度單位，只差換算倍率。取用軸與面積採 W×D、W×H 或 D×H，須由各 Type 的計量規則（ND-28）明列，不由系統猜測。** |
| ED-10 | `_13_備註` 的預設與 `20_DW` 操作說明                                                 | `我是備註，UCCU` 看似測試字串；`20_DW` 說明可移至獨立 instruction                                                     | **輕鬆建議**：備註預設空白，`20_DW` 操作說明移到 instruction。這主要影響資料整潔，不影響核心鏈。                                                                                                               | **輕鬆建議**：同意。補充兩點：`我是備註，UCCU` 會經 TagTrigger 寫進 91 個 layer 底下的**所有物件**並進 Registry，所以它是「散佈在正式資料裡的測試字串」，不只是 Dictionary 裡的一格；另外 `20_DW` 那列是真正的操作說明（「Block放在此圖層，資料寫在Block，不寫在子圖層」），必須有 instruction 欄位承接，不能一併清空。                   | 雙方均無強烈建議；共同方向是備註預設空白、操作說明移至 instruction。                 | **採用，但預設值不是空白而是提示字串 `(手動輸入備註)`：使用者看得出這格要自己填，也不會像 `我是備註，UCCU` 那樣像是誤留的測試字串。2026-08-14 已在字典執行：91 列 `我是備註，UCCU` 全部替換，`20_DW` 那列的真操作說明（`Block放在此圖層，資料寫在Block，不寫在子圖層`）原樣保留，待 instruction 欄建立後再移出。`_13` 屬 WHITE_LIST（物件已有值就不覆寫），所以提示字串只會出現在尚未填寫的物件上。** |
| ED-11 | `DNA_REF_` 參考線是否仍需要                                                        | Dict-to-Layer 每次建立帶完整 Dictionary UserText 的參考線且會累積；需確認原本用途                                         | **輕鬆建議**：日常 Dict-to-Layer 不再建立；若仍需要目視樣本，另做可重建、可清除的 Type Sample 指令。                                                                                                         | **一般建議**（提高）：不是因為風險高，而是因為這是整份清單裡**唯一「我們不知道它為什麼存在」**的項目。其他每一項都有明確的現況與後果，這一項只有觀察。建議移除前先請你說明原本用途，不要因為它看起來像殘留就砍掉。累積問題可以先用「重跑時先清除前次 `DNA_REF_`」單獨解決，去留可以晚點再決定。                                                                   | 已由現行程式確認：Dict. to Layer 會為每個 Dictionary row 建立 `DNA_REF_*` curve、放入對應圖層並附上 row UserText。 | **保留。用途是讓每個 Dictionary 圖層在 Rhino 場景中至少有一個物件，避免空圖層被 Purge。重跑必須依穩定 Type ID 更新或取代既有參考線，不得重複累積。** |
| ED-12 | Rhino 文件不是 cm 時如何處理                                                        | A. 直接阻擋；B. 明確換算並顯示；目前工具與常數全部按 cm 設計                                                                | **強烈建議**：2.0 初版直接阻擋並清楚說明；待全部量綱規則有測試後再考慮換算。這會限制非 cm 專案，但安全且可預測。                                                                                                             | **一般建議**（降低）：建議拆成兩件事。「啟動時偵測並明確顯示文件單位」是強烈建議且幾乎零成本；「非 cm 時直接阻擋」則只是目前最安全的**預設策略**，不是必要條件。實務上你所有專案都是 cm（每支工具的預設值都是 cm），阻擋的實際價值有限，卻會讓一個合法的 mm 專案完全打不開。先做偵測＋明確警告，阻擋與否日後可隨時調整。                                                     | 有衝突：CodeX 主張非 cm 直接阻擋；Claude 主張先警告。共同強烈底線是啟動時偵測並明確顯示單位。  | **部分採用：偵測並顯示單位；非 cm 不阻擋，但告知後續風險，並建議將檔案切換為cm單位**                                    |
| ED-13 | 複製 Sheet 時一般 Tag 是否保留原模型來源                                                 | 同一模型物件可能合理地出現在多張圖；但新 `sheet_id`／`drawing_id`／`tag_id` 必須建立，Index Tag 目標必須重審                        | **一般建議**：一般 Tag 保留 `source_object_id`，但建立新 Tag／Sheet／Drawing ID；Index Tag 清除或重新指定目標。代價是複製完成後需處理待確認清單。                                                                      | **強烈建議**（提高，僅限 Index Tag）：一般 Tag 保留來源屬一般建議，同意 CodeX。但 Index Tag 的處理屬強烈：現況複製 Layout 後 Index Tag 沿用來源頁的 `.Target_DV_ID`，Infuser 判定「找得到目標、綁定正常」而**不標紅**，於是新頁的剖面索引安靜地指向來源頁圖號。這是複核中唯一**現有任何檢查都抓不到**的錯誤，必須在複製當下就處理，不能留給事後 Health。 | 無衝突的強烈部分：複製時必須清除或重新指定 Index Tag 目標；一般 Tag 規則仍是一般建議。      | **採用：複製 Sheet 時建立新的 `sheet_id`、`drawing_id`、`tag_id`。除純手動 `TAG_DW` 外，所有 Tag 清除來源綁定、自動欄位與人工補充說明，由使用者重新綁定；lock 恢復該 Block template 的預設狀態。`TAG_DW` 的人工編號、寬、高及其他 Block 內容完整保留。**這是全表唯一由系統主動清除 manual content（`attr_manual_補充說明`）的動作，屬刻意例外**——複製出來的新頁不應帶著來源頁的註解；ECO-01、ED-02 與 E 區「不得覆寫人工欄位」的規則在其他所有情境仍然成立。Block template manifest 需另增「預設 lock 狀態」欄位，供複製與新建 Tag 時還原。** |
| ED-14 | 同款家具的多個 2D Block，能否共用同一個 `FF-01__Chair-1` 名稱 | 現況 `TAG_ITEM` 會把名稱拆成 `FF`／`01`／`Chair-1`；一般模型物件則讀 Dictionary `_03_ID編號`，兩套來源最後寫入相同顯示欄。已確認家具命名維持獨立，不併入 Dictionary Type ID。 | **一般建議**：同款家具共用名稱與代碼；例如十張相同椅子都可使用 `FF-01__Chair-1`。只有需要逐件追蹤時才另設 instance ID，不要靠 Block 名稱編流水號。 | **一般建議**：格式不符時停止並警告，避免 Tag 靜默抓錯名稱。 | 使用者已確認同款家具可共用名稱與代碼；若未來需要逐件追蹤，另設 instance ID。 | **採用：此格式只規範 2D 家具 Block，供 `TAG_ITEM` 取值；維持 `FF-01__Chair-1`，同款的多個家具 Block 可以共用同一名稱。名稱格式不符時停止並警告，不靜默抓錯值。因為共用名稱，家具 Tag 沒有 instance 級來源，故明列為 ECO-04 的正式例外：家具 Tag 以 `source_type: block_name` 綁定名稱而非 Object ID，Health 對它不判 `orphaned`（家具 Block 被刪或換掉，Tag 不會有反應），這是已接受的代價。** |
| ED-15 | 圖框 `03-A3 Scale` 在 2.0 要維持人工，還是由 Detail／Sheet metadata 自動產生                | 現況沒有 Python writer；key 同時混入面板排序 `03-`、圖幅 `A3` 與欄位語意 `Scale`，換圖幅就可能迫使 key 改名                        | **一般建議**：第一階段保留人工值，但 canonical ID 改為與圖幅、排序無關的穩定欄位；日後若自動化，再另定多 Detail 或不同比例的顯示規則。                                                                                           | **一般建議**：先拆 identity 與 label。是否自動算比例取決於一張 Sheet 是否可能有多個不同 Detail scale，需使用者補充工作方式。                                                                                                                                          | 無衝突：比例值維持人工輸入；canonical ID 與 `03-` 排序、`A3` 圖幅及顯示標籤分離。 | **採用：每張圖的比例維持人工寫入；canonical ID 改為與圖幅、排序無關的穩定欄位。** |
| ED-16 | Layout ID 是否只寫入 manifest 明列為圖框的 Block                                      | 1.x 把所有未列入 Data／Index／Elev 0 清單的 Block 都當圖框，寫入 `DWG_NO`／`DWG_NAME`；未知 Block 也會被改                   | **強烈建議**：只寫 `role: title_frame`，未知 Block 零寫入。否則新增任何普通 Block 都可能被靜默加上圖框資料。                                                                                                  | **強烈建議**：採明確 allow-list／manifest；若未宣告就報告並略過，不以「不認識」推論成圖框。                                                                                                                                                                   | 無衝突：只寫入 manifest 中的 `role: title_frame`；未知 Block 報告並略過。  | **採用**                                                                             |
| ED-17 | 2.0 資料化與發布的正式掃描範圍                                                          | 1.x TagTrigger 會處理全部 M3D 3D 物件，不受可見／鎖定狀態影響；若 2.0 改成只處理選取或可見物件，Impact Report 與 Registry 可能看似完整、實際漏件 | **強烈建議**：正式「全專案驗證／發布」預設掃描 M3D 正式範圍內全部 3D 物件；不符合 schema 的物件也要進報告並列為阻擋，而不是先被過濾掉。hidden／locked 只是 Rhino 編輯狀態，不是排除條件；可另提供選取／篩選模式做局部預覽或套用，但不能用局部結果宣告全專案可發布。正式排除要用可追蹤的 typed 狀態。 | 尚未提供；本項由本輪 CodeX 一致性補查新增，待日後 Claude 複核時再填，不代替它推測立場。                                                                                                                                                                         | 僅 CodeX 提供且為強烈建議，未發現既有衝突：正式發布掃描全部 M3D 正式範圍，局部模式不得宣告全案完成。 | **採用**                                                                             |
| ED-18 | Cabinet／BOM 是否加入 LoopFlow 2.0 | 已確認不加入 LoopFlow 2.0 主鏈，也不是 2.0 發布門檻；未來若仍需要，另作單純建模工具或獨立外掛。 | **一般建議**：核心工作鏈與安裝／migration 驗收通過後即可發布 2.0，不讓 Cabinet／BOM 阻擋。 | 尚未提供；本項由 CodeX 一致性補查新增，不代替 Claude 推測立場。 | 使用者已裁決，不再待決定。 | **不加入 2.0。未來可能改為單純建模工具或獨立外掛。** |

## D. Nexus／Dictionary 細項決策

本區承接 `Nexus與字典盤點.md` 的細部選項；該盤點只保存 1.x 證據、設定、衝突與風險，不再維護另一份可編輯答案。與上方 `ECO-*`／`ED-*` 重疊的列，在最右欄標示唯一裁決編號且不另填答案；只有標示 `待決定` 的 ND 列需要單獨回答。

### 第一輪：先固定資料骨架

| ID     | 要決定什麼                 | 選項                                                                                 | CodeX 建議                                           | AI 建議整合／衝突                                   | 你的決定／說明                                                                   |
| ------ | --------------------- | ---------------------------------------------------------------------------------- | -------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------- |
| ND-01  | 程式欄位 ID 與中英文顯示        | A. 固定英文 machine key，中文／英文只做顯示標籤；B. 完整中文 key；C. 保留任意字尾、只認 prefix                    | **強烈建議 A**；最適合穩定 schema 與多語文件                      | 僅 CodeX 提供且為強烈建議：採固定英文 machine key，語言只作顯示標籤。 | **採用 A**                                                                  |
| ND-02  | Dictionary 的責任        | A. layer／類型的預設資料；B. 同時保存每個物件的即時資料；C. 只做 layer 清單                                   | **強烈建議 A**；與 ECO-01 同步，物件即時資料留在 UserText／Registry  | 強烈建議與 ECO-01 無衝突：Dictionary 保存類型預設，物件現值另存。   | **採用 A；隨 ECO-01 統一裁決**                                                    |
| ND-03  | 欄位所有權                 | A. 每欄固定 `dictionary_default / computed / object_override / external`；B. 繼續按程式分支猜測  | **強烈建議 A**；這是 Nexus 可拆分的前提                         | 僅 CodeX 提供且為強烈建議：每欄明定 typed ownership。       | **採用 A**                                                                  |
| ND-04a | Rhino 模型文件單位          | A. 固定 cm，非 cm 直接阻擋；B. 支援任意模型單位並明確換算                                                | 現行工具全部按 cm 設計但從未驗證；與 ED-12 同步                      | ED-12 已裁決：啟動時偵測並顯示；非 cm 不阻擋，但警告風險並建議切換為 cm。           | 由 ED-12 統一裁決；本列不另填                                                        |
| ND-04b | `_08_單位` 與 `_09_實作數量` | A. 建立估算單位 enum 與單位→幾何量規則；B. `_09` 先定義為人工／外部值；C. 移除估算功能                             | **一般建議 B**；與 ED-04、ED-09 同步                        | 無強烈建議。                                       | 由 ED-04、ED-09 統一裁決；本列不另填                                                  |
| ND-05  | 缺值                    | A. 內部使用 typed null，只有畫面／Tag 顯示 `-`；B. 所有層都保存 `-` 字串                                | **強烈建議 A**；避免把缺值誤當真實文字或數字                          | 僅 CodeX 提供且為強烈建議：內部使用 typed null，`-` 只作顯示。   | **採用 A**                                                                  |
| ND-06  | Dictionary 中的計算欄      | A. 保留欄位定義，但 row value 不作預設；B. 從 Dictionary 移除；C. 允許 row 預設覆寫計算                     | **一般建議 A**；schema 可見、所有權仍清楚                        | 無強烈建議。                                       | **採用 A（已落實）**：保留 _01、_05～_07、_09、_11、_12 的欄位定義，但 Dictionary row 不提供預設值；實際值由模型判定、計算或系統產生。2026-08-14 核對，這七欄在字典 92 列全部留白 |
| ND-07  | Layer taxonomy        | A. 採用目前 Dropbox 中文版的中英雙語 layer path；B. 改為英文 path、中文只顯示；C. 另訂新分類                    | 已選定中文版作重構來源；仍須依 ECO-02 將 layer path 與穩定 Type ID 分離 | 本列未標強烈建議；ECO-02 的穩定 Type ID 原則已採用。           | 採用目前 Dropbox 中文版的中英雙語 layer path                                          |
| ND-08  | 專案檔案位置                | **已定案**：Dictionary 與即時交換 JSON 位於各專案的 Dropbox 工作檔根目錄，以環境變數解析；JSON 預設整理於 `exchange/` | 家中電腦使用相同變數名稱、不同實體路徑                                | 既有定案，無衝突。                                    | 已採用                                                                       |

### 第二輪：固定 Nexus 的核心語意

| ID    | 要決定什麼               | 選項                                                                                 | CodeX 建議                                             | AI 建議整合／衝突                                                    | 你的決定／說明                                        |
| ----- | ------------------- | ---------------------------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------- | ---------------------------------------------- |
| ND-09 | Space identity      | A. 穩定 Space ID + 顯示名稱；B. 只用名稱                                                      | **強烈建議 A**；改名不會破壞關聯，與 ED-07 同步                       | 僅 CodeX 提供且為強烈建議：Space 使用穩定 ID，名稱只作顯示。                        | **採用 A；多樓層、共邊、不可面積重疊與衝突行為均由 ED-07 統一裁決。**                        |
| ND-10 | Space 命中            | A. boundary 有 priority／樓層，跨界時回報衝突；B. 取第一個；C. 取最大重疊                                 | **一般建議 A**；需補充實際 boundary 是否重疊、是否多樓層，與 ED-07 同步      | 無強烈建議。                                                        | 由 ED-07 統一裁決；本列不另填                             |
| ND-11 | 高程 basis 值域         | A. 分別正式定義 BH／TH／BC／CH／`TH/BH`；B. 刪除不用的 basis；C. 延續現況                                 | **一般建議 A 或 B**；尤其要確認 `CH` 與非 Block 的 `BC`，與 ED-06 同步 | ED-06 已定義全部四種現行值；`TH/BH` 因字典不再使用而退場，等同採 A＋B。 | 由 ED-06 統一裁決；本列不另填。註：1.x 程式的合法值寫作 `TH/BH`（斜線，`LF_Nexus.py:179`），migration 比對時以程式字面為準 |
| ND-12 | 高程 datum            | A. boundary 用結構化 UserText 儲存 datum／type／level ID；B. 從 ObjectName 解析數字              | **強烈建議 A**；ObjectName 只作顯示，不承擔數值資料                   | 僅 CodeX 提供且為強烈建議：datum／type／level ID 使用結構化資料，ObjectName 只作顯示。 | **採用 A**                                       |
| ND-13 | W／D／H 方向            | A. 依物件 local frame／類型規則；B. 固定 World XYZ；C. 單純由三邊排序                                 | **一般建議 A**；實作時機與 consumer 需求依 ED-08                  | 無強烈建議。                                                        | 由 ED-08 統一裁決；本列不另填                             |
| ND-14 | UUID 範圍與複製          | A. 專案全域唯一，複製產生新 UUID；B. 只在每個 `.3dm` 唯一；C. 複製保留 UUID                                | **強烈建議 A**；Tag／Registry 的跨檔關聯另用 mapping，與 ECO-11 同步  | 強烈建議與 ECO-11 無衝突：專案全域唯一，複製產生新 UUID，跨檔關聯用 mapping。             | **採用 A；隨 ECO-11 統一裁決**                         |
| ND-15 | Construction Status | A. Dictionary 給預設、物件可 override，並提供 Reset to default；B. Dictionary 每次強制覆寫；C. 只由物件維護 | **一般建議 A**；與 ECO-01、ND-03 同步                         | 使用者採一個明確的分層規則：Dictionary 只為新圖層提供初始值；既有 Rhino 圖層與物件資料是現況真相，不反向覆寫 Dictionary。 | **採用自訂規則：Dictionary 中只有但 Rhino 尚不存在的新圖層，建立時取得 Dictionary `_02` 初始資料；Dictionary 與 Rhino 已有同名圖層時，保留 Rhino 圖層既有資料。`_02` 的 Instance 現值只由物件維護，同圖層物件可以有不同狀態；重新匯入 Dictionary 不覆寫物件的人工修改，也不把物件狀態寫回 Dictionary。** |
| ND-16 | Quantity            | A. 現在定義計算規則；B. schema 先保留但明確標為人工／外部值；C. 2.0 移除                                     | **輕鬆建議 B**；與 ED-09 同步                                | 無強烈建議。                                                        | 由 ED-09 統一裁決；本列不另填                             |
| ND-17 | Remarks 預設          | A. 預設空白；B. 保留 `我是備註，UCCU`；C. 每個 Type 另訂正式備註                                        | **輕鬆建議 A**；`20_DW` 正式操作說明另設 instruction，與 ED-10 同步   | 無強烈建議。                                                        | 由 ED-10 統一裁決；本列不另填                             |

### 第三輪：固定下游交換與延後功能邊界

| ID    | 要決定什麼                      | 選項                                                                              | CodeX 建議                                          | AI 建議整合／衝突                                                    | 你的決定／說明                                                                 |
| ----- | -------------------------- | ------------------------------------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------- | ----------------------------------------------------------------------- |
| ND-18 | Registry payload           | A. 版本化 typed schema + 明確 extension 區；B. 將全部 UserText 原樣快照                       | **強烈建議 A**；與 ECO-06 同步                            | 強烈建議與 ECO-06 無衝突：Registry 使用版本化 typed schema 與明確 extension 區。 | **採用 A**                                                                |
| ND-19 | `_03` 與家具 `FF-01` 的 Tag 語意 | A. Dictionary 類別碼／序號拆成 typed 欄位，家具另依 ED-14 定義 namespace；B. 全部固定同一連字號字串；C. 不再拆分  | **強烈建議 A**；與 ED-01、ED-14 同步                       | 強烈建議與 ED-01 無衝突：Dictionary 欄位拆分；家具維持獨立 namespace。     | **採用 A；家具同款可共用名稱與代碼，逐件追蹤另用 instance ID。**                                               |
| ND-20 | Tag 鎖定欄位                   | A. 單一 canonical boolean／enum，由 UI 切換；B. 繼續讓使用者輸入文字                              | **強烈建議 A**；舊 key／其他值只由 migration 列為待確認，與 ED-02 同步 | 強烈建議與 ED-02 無衝突：UI 寫入 canonical boolean／enum，舊值交由 migration。  | **採用 A；隨 ED-02 統一裁決**                                                   |
| ND-21 | `Layer to Dict`            | A. 明確只匯出 layer defaults，另做 object data export；B. 改為彙總 object UserText；C. 取消反向匯出 | **一般建議 A**；避免 layer 與 object 資料混為一談               | 無強烈建議。                                                        | **採用 A：匯出 Rhino layer defaults 與新增、刪除、改名、修改等差異，不彙總 Object UserText。產生獨立比較用 XLSX，在底部列出變動圖層，由使用者人工核對並更新正式 Dictionary；不得自動覆寫正式 Dictionary。** |
| ND-22 | `20_DW` 特例                 | A. 保留單一 DW 類型及 child-layer 排除規則；B. 每個 DW child 都進 Dictionary；C. 重新分類            | 需依目前門窗工作方式決定                                      | 無強烈建議。                                                        | **採用 A，維持 1.x 的單一 `20_DW` 類型與 child-layer 排除規則。** |
| ND-23 | Cabinet `_CB.*`            | A. 現在凍結四欄語意，程式延後；B. 等 Cabinet 重構時再決定                                            | **已裁決 B**：Cabinet／BOM 移出主鏈，四欄語意留給 Cabinet 工作軌     | 四欄已於 2026-08-14 從字典移除，本題不再有待定義的對象。               | **已採用 B；欄位已從字典移除，2.0 schema 不含 `_CB.*`。**                    |
| ND-24 | Cabinet 方向                 | A. L／W／T 依 panel local frame；B. 依三邊大小排序；C. 依 current layer／類型各自規則               | 技術上建議 A，但已延後到 Cabinet 工作軌，不阻擋核心契約                 | ED-18 已裁決 Cabinet 不加入 LoopFlow 2.0。                       | **不屬 LoopFlow 2.0；若未來成為獨立建模工具／外掛，再於該專案採 local frame 原則重新定義。** |
| ND-25 | Dictionary 驗證強度            | A. 重複、未知欄、錯型別、錯單位直接阻擋並列清單；B. 警告後盡量執行                                            | **強烈建議 A**；核心欄位嚴格驗證，未知 extension 另設允許區            | 僅 CodeX 提供且為強烈建議：核心 schema 錯誤直接阻擋，未知擴充只允許在 extension 區。       | **採用 A**                                                                |
| ND-26 | 正式資料化／發布 scope             | A. M3D 正式範圍內全部 3D 物件，另提供局部預覽；B. 只處理選取；C. 只處理可見／未鎖定                              | **強烈建議 A**；schema 不合格者在報告中阻擋發布，與 ED-17 同步         | 強烈建議與 ED-17 無衝突：正式發布掃描全部 M3D 正式範圍，局部模式只作預覽。                   | **採用 A；隨 ED-17 統一裁決**                                                   |
| ND-27 | Cabinet 與 2.0 首發邊界         | A. 核心 2.0 可先發布，Cabinet 後續加入；B. Cabinet 雖延後仍是首發門檻                                | **一般建議 A**；避免零碎 BOM 阻擋主鏈，與 ED-18 同步               | ED-18 已明確裁決 Cabinet 不加入 LoopFlow 2.0。 | **不屬 2.0 首發或後續核心範圍；未來若需要，改為單純建模工具或獨立外掛。** |
| ND-28 | Dictionary 新增「計量規則」欄要填什麼 | A. 單一封閉詞彙只回答「用哪個軸」，量綱由 `_08_單位` 決定；B. 直接寫算式字串；C. 不建欄位，由程式依 Type 硬編碼 | **建議 A**；算式字串無法驗證，硬編碼會讓新增 Type 必須改程式 | 由 ED-09 衍生；ED-08 已裁決軸向固定，A 才成立 | **採用 A。詞彙：`COUNT`／`LEN_W`／`LEN_D`／`LEN_H`／`AREA_WD`／`AREA_WH`／`AREA_DH`／`VOL_WDH`。驗證：token 量綱必須與 `_08_單位` 量綱相符，不符直接阻擋（依 ND-25）。缺值：留空則 `_09` 不計算、寫 `-`、在驗證報告列為「計量規則未定義」，不猜測。92 筆 Type 已全部填寫完成（92/92），並驗證為 0 筆量綱不符；其中 41 筆為計數、20 筆為長度、29 筆為面積、2 筆為體積。**<br>**存放位置：主表第 10 欄，欄名 `(單位計量規則)`，位於 `_08_單位` 與 `_09_實作數量` 之間**（使用者裁決，2026-08-14；原本暫置的第二個工作表已移除）。選這個位置是為了維護便利——規則就在它所依據的單位旁邊。**1.x 的行為不是這個決定的代價**（見 E 區「1.0 的定位」）：1.x 會把這欄寫進每個物件與 `DNA_REF_`、經 Push 進 Registry，並在重跑 TagTrigger 前把所有物件判為 error（`LF_Nexus.py:397, 498`、`LF_Push_3D_to_JSON.py:112`）。這是舊路的鋪法，記在這裡只作 migration 掃描的辨識依據，不構成 2.0 的設計約束（2.0 不預設過渡期，見 E 區「1.0 的定位」）。2.0 由欄位 role 決定誰下放到物件，與欄名前綴無關。<br>**為什麼不能只靠 `_08_單位` 推導**（2026-08-14 以 92 列實填值驗證）：單位只決定量綱，不決定軸向。`坪` 分裂為 `AREA_WD` 13 筆與 `AREA_WH` 8 筆，`才` 分裂為 `AREA_WH` 7 筆與 `AREA_WD` 1 筆——`CL-05 天花油漆` 與 `WL-10 牆面油漆` 同為「坪」卻是不同軸向，`FL-07 石材地坪` 與 `WL-13 石材牆面` 同為「才」亦然。嚴格說只有 29 筆面積列帶著無法推導的資訊（其分野是水平面對垂直面），41 筆計數與 2 筆體積由單位完全決定，20 筆長度目前一律 `LEN_W` 但那是現況而非法則。**仍決定 92 列全填**：推導值出錯是無聲的（改了單位就會安靜換掉計算方式），兩邊都存則 validator 可交叉比對並阻擋不一致；這份冗餘買到的是「單位與規則任一邊被改動，另一邊會揭發它」。8 個 token 也保留了 `LEN_H`、`AREA_DH` 等目前用不到的組合的表達能力。 |

> 本區是從既有 ND 菜單移入，沒有臆測第二位 AI 未曾提供的立場。Claude 已明確提出的重疊意見保留在對應的 `ECO-*`／`ED-*` 欄；未提供者維持空白事實，不冒名補寫。

## E. 已確認、不需在本表重複決定

- **Cabinet 與 BOM 不加入 LoopFlow 2.0**。1.0 的 BOM 功能過於零碎，混入主鏈會汙染核心資料契約。因此 2.0 主鏈的 Nexus、Registry、Tag 與 Health 都不處理 `_CB.*`，也不得為 Cabinet 增加 layer 判定分支；櫃體在主鏈中就是一般 3D 幾何。未來若仍需要 Cabinet，改為單純建模工具或獨立外掛，另行建立契約與開發路徑。
- **`04_CB_櫃體` 圖層群組保留，這不牴觸上一條。** 兩者是不同層次：`04_CB_櫃體` 的 7 筆 Type（`Color.純色`、`Glass.玻璃`、`Hardware.五金`、`Metal.金屬`、`Mirror.鏡面`、`Stone.石材`、`Timber.木紋`）是**櫃體的材質分類**，讓櫃體幾何能像牆、地坪一樣取得 Type、Space、高程與 UUID；離開 2.0 的是 `_CB.01`～`_CB.04` 這組**板件製作資料**。看到字典裡仍有 `04_CB_櫃體` 不代表 Cabinet／BOM 回到 2.0。
- **字典現況（2026-08-14 核對，含當日 ND-28 併欄後的最終狀態）**：`LoopFlow Dictionary v2.0`，**15 欄**、92 列、12 個類別碼、12 個頂層群組（編號與 1.x 相同）。相對 1.x 的 18 欄，移除的是 `_CB.01`～`_CB.04`（4 欄），新增的是 `(單位計量規則)`（1 欄），故 18 − 4 + 1 = 15。`_10_高程基準` 實際只有 BH 50、TH 18、CH 8、BC 16 四種，`TH/BH` 已退場。`_01`、`_05`～`_07`、`_09`、`_11`、`_12` 七欄 92 列全部留白，ND-06 已在字典落實；`(單位計量規則)` 則依 ND-28 全數填妥（92/92，0 筆量綱不符），是 `_09` 未來計算時依據的規則。
- **1.0 的定位（2026-08-14 強化）**：**2.0 不再把 1.0 當成設計約束。** 1.0 的價值在於它證明了這條路走得通——從 Dictionary 到建模、資料化、發布、剖面、Layout、Tag 到出圖，整條鏈確實能產出圖說；但它鋪得崎嶇，現在是把路翻掉重建，不是沿著舊路面補。因此：
  - 1.x 的實作行為（欄位怎麼寫入、清單怎麼硬編碼、顏色怎麼當狀態）是**現況證據與 migration 掃描依據**，不是 2.0 的設計輸入，也不能用來評價 2.0 的決定。
  - 要保留的是**已驗證可行的工作邏輯與使用者控制點**：何時寫入、何時發布、切哪張圖、Tag 綁誰、同步範圍。
  - 「1.x 會因此壞掉／變慢／多寫一欄」不構成反對 2.0 設計的理由；只有「使用者實際做不到某件事」才是。
  - **不預設過渡期**：2.0 從架構開始重建，指令逐一建立與測試（見 `_LoopFlow_系統設定.md`「重構期間的 Rhino 測試入口」與 `開發任務與路徑.md` 的 LF-D01～E04）。1.x 不是並行運作或需要相容的對象，因此不為它保留欄位、分支或行為。
- **1.0 操作說明的地位**：`loopflow_1.0_workflow_YT.txt` 是操作邏輯參考，不是流程契約。1.0 的用法有一些限制但沒有造成大問題；2.0 的步驟順序應配合新程式架構重新思考。必須維持的是使用者的控制意圖（何時寫入、何時發布、綁誰、同步範圍），不是按鈕順序。要改掉某項 1.0 行為是允許的，但必須是有意識的裁決並記錄，不能在搬程式時順手改掉。
- `TAG_DW` 已改為純手動 Tag：`attr_dw_id`、門窗寬與門窗高都由使用者輸入，不建立來源、不由 Sync 覆寫，也不因沒有來源被 Health 判成 unbound。
- 10 份實際 Block 文字（9 Tag、1 圖框）已由 Rhino instance 擷取，共 24 個唯一 UserText key；文字與欄位所有權盤點以現行 v2 workflow simulation 為準。
- 2.0 正式版以完整安裝檔／可安裝套件交付；開發期才使用逐支 entrypoint 測試按鈕。
- 工具列保留 Rhino Section 快捷入口；Macro 直接呼叫 Rhino 8 內建指令，不建立 Python entrypoint、不封裝 Rhino Section 功能本體。
- 正式安裝不覆蓋使用者完整 Rhino workspace。
- 實際封裝技術、工具列格式與圖示來源延後至發佈階段評估。
- Dictionary 與即時交換 JSON 使用 Dropbox 工作檔根目錄，實體路徑由每台電腦的環境設定解析。

## F. AI 回寫檢查

使用者修改本文件後，AI 必須：

1. 逐項區分「已明確決定」、「仍待決定」與「答案會影響其他項目」。
2. 不擅自補完使用者沒有回答的語意。
3. 將已決定內容回寫 `資料生態藍圖.md` 與 `_LoopFlow_命名與資料契約.md`。
4. 需要補充 1.x 證據時更新 `Nexus與字典盤點.md`，但不在盤點檔新增第二份答案。
5. 更新 `開發任務與路徑.md`、`重構進度.md`、fixtures 與 migration 影響。
6. 回寫完成後保留本文件的決策結果與日期，作為可追溯紀錄。

## G. 依目前 AI 建議推導的完整工作流程

兩份模擬執行流程（CodeX 版與 Claude Code 版）已移至獨立文件，避免本決策表過長而不利填寫：

- Markdown：`工作流程模擬.md`
- 深色好讀版：`工作流程模擬.html`（同目錄，由 Markdown 產生）

2026-08-12 的初版提案已刪除；上述 MD／HTML 是唯一現行版本。

該文件把本表的建議轉成可實際理解的操作鏈，供你判斷這些原則是否符合工作習慣。它不維護任何決策答案；`ECO-*`／`ED-*` 的裁決一律填在本表。
