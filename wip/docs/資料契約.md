# LoopFlow 2.0 — 資料契約

本文件是 2.0 Dictionary、模型資料、Registry、View、Drawing、Sheet、Tag 與 Health 契約的唯一來源。前期決策已封存在 `前期規劃/資料生態決策表.md`；實作不得再從前期討論文字推測答案。

## 契約狀態

- ECO-01～11、ED-01～18、ND-01～28 已裁決。
- 本文件固定資料語意、所有權、值域、序列化名稱與驗證規則。
- LF-A03 已鎖定 machine key、身分格式、layer path、指令與檔名。
- LF-A04 已鎖定 Registry 物件欄、10 份 Block template manifest、24 個 legacy key 的 owner 與 migration mapping。
- LF-A06 已鎖定可機器驗證的 fixtures；檢查器為 `wip/tools/check_contract_fixtures.py`。第一條規則是計量規則與單位量綱一致（Type Catalog 給後續 GH 使用；2.0 不算尺寸／數量）。
- 正式專案資料仍不得寫入，直到對應功能通過驗收。
- Machine key 固定使用英文；中文／英文只作顯示標籤。日後正式發布會有 tw／en 兩套使用者文字（指令說明、選單、彈窗、Dictionary 顯示欄名），載入仍對到同一組英文 machine key。開發期只維護繁中，本階段不做語系切換。

## 共通規則

| 項目 | 規則 |
|---|---|
| 缺值 | 內部使用 typed null；只有 Tag／報表顯示 `-` |
| 版本 | Dictionary、Registry、manifest 與跨程序 payload 必須有 `schema_version` |
| 未知版本 | 停止並說明，不猜測解析 |
| 核心錯誤 | 重複、未知核心欄、錯型別、錯單位、錯量綱直接阻擋 |
| 擴充 | 只允許放在明確的 extension 區，不把任意 UserText 升格為 API |
| ID | 專案範圍唯一；複製產生新 ID；跨檔關聯另用 mapping |
| 顯示名稱 | 可改名，不承擔穩定身分 |
| 人工內容 | 除本文件明列的 Duplicate Sheet 特例外，Sync、Repair、migration 與錯誤處理不得覆寫 |
| 雙寫 | 日常指令只寫 2.0 canonical key；1.x 名稱只由獨立 migration 辨識 |

## 序列化慣例

- JSON、in-memory schema 與文件層欄位使用英文 `snake_case`，對齊本文件的概念名。
- 3D 物件 UserText 使用編號中文 key（`_01_空間名稱*`、`_07_UUID`…），`_01`～`_08` 對齊 Dictionary 圖面欄、一個編號只給一個 key，內部欄位接在 `_09` 之後。可手動欄的鍵名帶 `*`；Dictionary Excel 欄名不加 `*`。`Q_01`～`Q_06` 是數量欄，只存在 Dictionary，2.0 不寫進物件。JSON 詞幹仍為英文。
- 圖層與文件 UserText 維持 `lf_` 前綴（`lf_type_id`、`lf_project_id`），避免撞上物件的 `_07_UUID` 或其他外掛。
- 同一語意在 JSON 與物件 UserText 對照：JSON 為 `object_id`，物件 UserText 為 `_07_UUID`。
- `schema_version` 為正整數，從 `1` 起跳，獨立於產品版號 `v2.0.0`。
- 每個持久化 payload 還必須有 `schema_id`，用來分辨種類；未知 `schema_id` 視同未知版本。

| schema_id | 用途 | 目前 schema_version |
|---|---|---|
| `loopflow.project` | Rhino 文件上的專案身分 | `1` |
| `loopflow.dictionary` | Type Catalog 載入後的正規化資料 | `1` |
| `loopflow.object` | 3D 物件 instance 現值 | `1` |
| `loopflow.space` | Space Boundary | `1` |
| `loopflow.local_frame` | 尺寸用的持久化座標框 | `1` |
| `loopflow.view` | Section／View 註冊 | `1` |
| `loopflow.drawing` | 可編輯 Drawing 與 provenance | `1` |
| `loopflow.sheet` | Sheet metadata | `1` |
| `loopflow.registry` | 發布快照 | `1` |
| `loopflow.tag_template` | 單一 Tag／圖框 manifest | `1` |
| `loopflow.tag_template_set` | 10 份 manifest 的集合檔 | `1` |

### 身分格式

| 欄位 | 格式 | 規則 |
|---|---|---|
| `project_id`、`object_id`、`space_id`、`level_id`、`view_id`、`drawing_id`、`drawing_element_id`、`sheet_id`、`tag_id` | UUID v4，canonical 小寫 `8-4-4-4-12` | 專案範圍唯一；複製產生新 ID；1.x 大寫 UUID 由 migration 正規化 |
| `type_id` | `{type_category}-{type_sequence}` | 不是 UUID。載入時用 Dictionary 的 12 個類別碼當前綴拆分，禁止只切第一個 `-` |
| `type_category` | 12 個類別碼之一 | 清單由現行 Dictionary 凍結，A06 fixtures 固化 |
| `type_sequence` | 非空字串 | 不含用來組 `type_id` 的那一個連接 `-` |
| `registry_revision` | 每個 `project_id` 從 `1` 起的正整數 | 發布成功才遞增 |
| `EXT` | 保留字 | 未命中 Space 時的唯一 `space_id`；不是 UUID |

### 檔案與路徑

Dictionary 與交換檔只經 `LOOPFLOW_WORKFILES_ROOT` 解析，不寫死磁碟機。

| 用途 | 路徑 |
|---|---|
| Dictionary | `%LOOPFLOW_WORKFILES_ROOT%\LoopFlow_Dictionary.xlsx` |
| Registry 正式檔 | `%LOOPFLOW_WORKFILES_ROOT%\exchange\<project_id>\Project_Registry.json` |
| Registry lock | `%LOOPFLOW_WORKFILES_ROOT%\exchange\<project_id>\Project_Registry.lock` |
| Registry pending | `%LOOPFLOW_WORKFILES_ROOT%\exchange\<project_id>\Project_Registry.pending.json` |
| Registry last-good | `%LOOPFLOW_WORKFILES_ROOT%\exchange\<project_id>\Project_Registry.last-good.json` |

- `project_id` 寫在 Rhino 文件 UserText `lf_project_id`，不從 `.3dm` 檔名推導。
- 缺少環境變數、目錄或 `project_id` 時停止，不猜測路徑、不建立正式 Registry。
- log 檔名可為進階設定；Registry 檔名是契約，不是使用者偏好。

### 指令 ID

開發期入口檔名即 command ID，對使用者的按鈕顯示名稱可另譯，但不得另建第二套業務邏輯檔。Scan／Apply／Verify 是 `LF_Nexus` 內的步驟，不另開 `.py`。

```text
LF_Nexus
LF_Dictionary_Editor
LF_Data_Viewer
LF_Push_3D_to_JSON
LF_Tagger_Grab
LF_Tagger_Laser
LF_Tagger_Index
LF_Tagger_Layout_ID
LF_TAG-O
LF_Infuser_Part
LF_Infuser_All
LF_Anchor_Frame
LF_Extract_CP
LF_Duplicate_Layout
LF_Sync_Worksession
```

三個獨立 2D 工具入口（`LF_2D_Cabinet_Gen`、`LF_2D_Shelf_Gap`、`LF_2D_DW_Gen`）不屬核心主鏈，啟動該工作軌時再鎖定。`LF_Cabinet_Suite` 不屬 2.0。

### Layer path

- 使用完整 path，分隔符為 `::`；terminal name 不得單獨當身分。
- 大小寫與空白以 Dictionary 的 `__Rhino Layer` 為準，程式不得自行折疊或補空白。
- 3D 正式範圍前綴預設 `M3D`，可改成這份 `.3dm` 記住的專案名稱（文件 UserText `lf_layer_prefix`）。2D 前綴仍為 `M2D`。更改前綴不會自動改名既有 layer。
- 系統資料層為契約，不是可隨意改的顯示偏好；根名稱隨專案前綴變化：

```text
<專案名稱>::_Data::Space_Boundaries
<專案名稱>::_Data::Level_Boundaries_FFL
<專案名稱>::_Data::Level_Boundaries_FL
<專案名稱>::20_DW
M2D::Anchor_Frame
```

未寫入前綴時 `<專案名稱>` 即 `M3D`。

- `DNA_REF_` 參考點依 `type_id` 更新或取代，名稱不得只依 layer 字串累加。
- Type layer 顯示色依圖層代號前綴（1.x 顯示慣例）；材質名稱為去掉 `M3D::` 的相對路徑（保留一個父圖層），顏色與圖層相同。不寫入物件 instance 顏色。
- `2D_D1`、`3D_W1` 只供獨立 2D 工具解析，主鏈不當身分。家具 `FF-01__Chair-1` 見 ED-14。

## Dictionary／Type Catalog

工作來源：`%LOOPFLOW_WORKFILES_ROOT%\LoopFlow_Dictionary.xlsx` 中文版本。

2026-08-14 已驗證狀態：`LoopFlow Dictionary v2.0`，15 欄、92 筆 Type、12 個類別碼、12 個頂層群組；`(單位計量規則)` 為 92/92 已填、0 筆量綱不符。

### 欄位角色

| 現行顯示欄 | 2.0 概念欄位 | Role／Owner | 規則 |
|---|---|---|---|
| `__Rhino Layer` | `layer_path` | Type mapping | 人類分類入口，不是 Type 主鍵 |
| `_01_空間名稱` | `space_id`／`space_display` | computed instance | Dictionary row 不提供預設 |
| `_02_建構狀態` | `construction_default` | Type default | 只為新建 Rhino layer 提供初始值 |
| `_03_ID編號` | `type_category`＋`type_sequence` | Type identity | Excel 可維持單欄；載入時嚴格拆分並驗證 |
| `_04_ID名稱` | `type_display_name` | Type display | 非唯一、可改名 |
| `_05_高程基準` | `elevation_basis` | Type rule | 2.0 只允許 BH／TH／CH／BC |
| `_06_高程計算` | `elevation_value`／`elevation_display` | computed instance | typed 數值與顯示字串分離 |
| `_07_UUID` | `object_id` | system identity | 不接受 Dictionary 預設或靜默重建 |
| `_08_備註` | `remarks_default`／`remarks` | Type default／instance override | 預設提示為 `(手動輸入備註)` |
| `Q_01_寬度W` | `dimension_w` | GH 後續 | Dictionary row 留白；2.0 不寫物件 |
| `Q_02_深度D` | `dimension_d` | GH 後續 | 同上 |
| `Q_03_高度H` | `dimension_h` | GH 後續 | 同上 |
| `Q_04_單位` | `estimation_unit` | Type rule／GH | 工程估算單位，不是 Rhino 文件單位；2.0 不下放物件 |
| `Q_05_計量規則` | `measurement_rule` | Type rule／GH | 2.0 不下放物件；量綱仍須與單位一致 |
| `Q_06_實作數量` | `quantity` | GH 後續 | Dictionary row 留白；2.0 不計算、不寫物件 |

`_CB.01`～`_CB.04` 已移除，不屬 2.0 schema。`20_DW` 的正式操作說明日後移到 Type instruction；在 instruction 欄建立前保留原文字。

Excel 維持中文顯示欄名；載入後必須變成上表的英文 machine key。欄名、欄數或類別碼與 schema `1` 不符時停止，不靠 prefix 猜欄。

### 字型

- 繁中版 Dictionary（現行工作來源與匯出）：微軟正黑體 10。
- 英文版 Dictionary（日後發布）：Arial 10。
- 本階段只維護繁中；匯出檔沿用繁中字型。程式常數為 `DICTIONARY_FONT_NAME`／`DICTIONARY_FONT_NAME_EN`。

### Dictionary → Rhino 合併

- Dictionary 有、Rhino 尚無的 layer：建立 layer，帶入 Type mapping 與 `_02` 初始資料。
- Dictionary 與 Rhino 已有同名 layer：保留 Rhino layer 既有資料。
- `_02` instance 現值只由物件維護；同 layer 物件可以不同。
- 重新匯入 Dictionary 不覆寫物件人工修改，也不把物件現值回寫 Dictionary。
- `DNA_REF_` 參考點放在原點，以避免空 Type layer 被 Purge；依穩定 `type_id` 更新或取代，不得累積。
- `20_DW` 維持單一 Type 與 child-layer 排除規則。

### 物件 instance 核心欄

3D 物件 UserText 只寫圖面需要的 canonical key。`Q_01`～`Q_06`（寬深高、單位、計量規則、實作數量）留在 Type Catalog，2.0 不下放、不計算。

`_01`～`_08` 與 Dictionary 圖面欄一對一，一個編號只給一個 key；內部欄位接在 `_09` 之後。

| UserText key | JSON key | 型別 | Owner | 規則 |
|---|---|---|---|---|
| `_01_空間名稱*` | `space_display` | string | computed | 顯示名稱；`EXT` 時顯示 `EXT`。空間框上可手動改，物件由 Apply 複製。中文名稱如「廊道」「衛浴」合法 |
| `_02_建構狀態*` | `construction_status` | string | instance override | 新 layer 才從 `construction_default` 初值；之後只由物件維護 |
| `_03_ID編號` | `type_id` | string | Type reference | 必須能在 Dictionary 找到 |
| `_05_高程基準` | `elevation_basis` | enum | computed snapshot | Apply 時從 Type 複製，供 stale 比對 |
| `_06_高程計算` | `elevation_value` | number | computed | 命中空間後＝高程框 `_15` ＋（物件取樣 Z − 框曲線 Z）。`_05` 決定取樣點。未命中（`EXT`）或找不到高程框時用世界 Z |
| `_07_UUID` | `object_id` | UUID | system | 不接受 Dictionary 預設；碰撞時預覽 mapping，禁止靜默換號 |
| `_08_備註*` | `remarks` | string | instance override | 新物件可用 `(手動輸入備註)`；已填不覆寫 |
| `_09_空間ID` | `space_id` | UUID 或 `EXT` | computed | 未命中為 `EXT` |
| `_10_樓層ID` | `level_id` | UUID | computed | Space boundary 帶入 |
| `_11_類型類別` | `type_category` | string | Type reference | 與 `type_id` 拆分結果一致 |
| `_12_類型序號` | `type_sequence` | string | Type reference | 與 `type_id` 拆分結果一致 |
| `_13_高程顯示` | `elevation_display` | string | computed | 顯示字串，不回解析成數值 |
| `_14_資料版次` | `data_revision` | integer | system | 第一次 Apply 若空才寫 `0`，之後 Apply 不覆寫。成功發布後寫入該次 `registry_revision` |
| `_15_樓層高程*` | `level_datum` | string | 高程框 | 只寫在高程框，不寫 3D 物件。數字字串如 `0`／`320`。讀取時若沒有此 key，才退回 ObjectName（舊檔相容）。可手動改；再 Apply 後該層物件 `_06` 跟著更新 |

Rhino 物件 UserText 用上表編號中文 key，讓面板依 `_01`、`_02`… 排序，且不得出現重複編號。可手動欄（`_01`／`_02`／`_08`／`_15`）鍵名帶 `*`；`_14` 不加。Dictionary Excel 欄名不加 `*`。JSON／Registry 仍用英文。讀取時若沒有新 key，仍可讀無 `*` 的同名舊鍵、舊的 `lf_*` 與 2.0 開發期編號（`_10_高程基準`、`_11_高程計算`、`_12_UUID`、`_13_備註`、`_14_空間ID`、`_15_樓層ID`、`_16_類型類別`、`_17_類型序號`、`_18_高程顯示`、`_20_資料版次`，以及更早的 `_01_空間ID` 等）。Apply 會寫新 key、清掉舊 key，並清掉尺寸／數量／座標框殘留（`_05_寬度W`、`Q_01_寬度W`、`lf_local_frame` 等）。圖層與文件 UserText（`lf_type_id`、`lf_project_id`）維持 `lf_`。

### Rhino → Dictionary 差異匯出

- 選單「寫回字典」在 Dictionary 同一目錄新增 `LoopFlow_Dictionary_Export.xlsx`。
- 第 1 列紅色提示（字高 20）：此檔只供核對，不能當正式字典、不可覆寫 `LoopFlow_Dictionary.xlsx`；`added_in_rhino` 合併時必須給新的 `_03_ID編號`。
- 第 2 列標題、第 3 列欄名。正式 Dictionary 不加提示列。
- 欄位對齊正式 Dictionary 的 15 欄，另加 `diff_status`（`unchanged`／`modified`／`missing_in_rhino`／`added_in_rhino`）。匯出檔不可當正式字典開啟。
- 整份字體、字高跟正式 Dictionary 相同（繁中微軟正黑體 10，英文版日後用 Arial；提示列除外）。只把有變動的 `diff_status` 文字換色，不改整列底色：`missing_in_rhino` 紅、`added_in_rhino` 藍、`modified` 橙；`unchanged` 維持預設黑。
- 只匯出 layer defaults，以及新增、刪除、改名、修改差異；不彙總 Object UserText。
- `added_in_rhino` 列在底部。新圖層若沿用舊 `lf_type_id`，合併前必須改成尚未使用的編號，否則正式字典會因 `duplicate_type_id` 無法載入。
- 由使用者人工核對與更新正式 Dictionary；不得自動覆寫。

## 模型單位與數量欄（2.0 不算尺寸）

2.0 只要圖面表達正確：高程、材料編號、空間。寬深高、面積、長度、實作數量留給後續 Grasshopper 可視化數量計算，不進 Nexus Scan／Apply，也不進 Registry `objects[]`。

### 文件單位

- 啟動時必須偵測並顯示 `ModelUnitSystem`。
- 非 cm 不阻擋，但要明確警告後續風險並建議切換為 cm。

### Dictionary 數量欄（給 GH）

`Q_01`～`Q_06` 留在 Type Catalog。`Q_04_單位` 與 `Q_05_計量規則` 是 Type 規則，載入時仍檢查量綱一致。`Q_01`～`Q_03`、`Q_06` 為計算欄，Dictionary row 留白。

允許 token（GH 後續使用；2.0 只驗證、不求值）：

```text
COUNT
LEN_W
LEN_D
LEN_H
AREA_WD
AREA_WH
AREA_DH
VOL_WDH
```

- Token 量綱必須與 `estimation_unit` 一致，不符直接阻擋 Dictionary 載入。
- 規則留空時警告「計量規則未定義」，不阻擋載入。
- `坪 = 指定面積（m²）× 0.3025`。
- `才 = 指定面積（cm²）÷ (30.3 × 30.3)`，即 1 才＝918.09 cm²。

| 規則 | 量綱 | 允許單位 |
|---|---|---|
| `COUNT` | count | 樘、片、組、台、座 |
| `LEN_W`／`LEN_D`／`LEN_H` | length | cm、mm |
| `AREA_WD`／`AREA_WH`／`AREA_DH` | area | 坪、才 |
| `VOL_WDH` | volume | m3 |

現行 Dictionary baseline 92 列只用到 `COUNT`、`LEN_W`、`AREA_WH`、`AREA_WD`、`VOL_WDH`。換算常數與 token 契約保留給 GH，不在 2.0 從模型幾何求值。

## Space 與高程

### Space

- 每個 boundary 帶穩定 `space_id`、`level_id` 與顯示名稱；UserText 為 `_09_空間ID`、`_10_樓層ID`、`_01_空間名稱*`。登記時曲線會放到 `M3D::_Data::Space_Boundaries`。中文名稱合法。
- 只有 `M3D::_Data::Space_Boundaries` 圖層上的曲線算 Space。畫在別層的曲線要先跑登記（選單 4）讓程式搬層，不在命中階段替使用者猜。
- 高程框與空間框**不寫入物件名稱**。高程框寫 `_10_樓層ID`、`_15_樓層高程*`；空間框寫 `_09_空間ID`、`_10_樓層ID`、`_01_空間名稱*`。讀樓層高程時若沒有 `_15_樓層高程*`，才退回無 `*` 舊鍵或 ObjectName（舊檔）。
- 登記高程框：先彈出清單點選 FFL 或 FL，選線後彈出視窗輸入高程。登記空間框：可複選，彈出視窗輸入同一個空間名稱。選線只用曲線過濾（對齊 1.x），不鎖全檔非曲線物件；已鎖定／隱藏的封閉曲線會先解開以便選到。兩步完成後曲線會搬到對應圖層。
- 登記時不必手填 `_10_樓層ID`。程式用空間框與高程框配對：高程差在 ±20 模型單位內（文件建議 cm 時即 ±20 cm），且空間多邊形整圈在高程多邊形內（共邊可）。對到後沿用或新建高程框上的 `_10_樓層ID`。
- FFL 與 FL 都算高程框；同一平面兩者都符合時優先 FFL。對不到或同時對到多個同條件高程框則停止。
- 同一 `.3dm` 可有多樓層。
- 相鄰 boundary 可以共邊；**同一 `level_id`** 的內部面積不可重疊。不同樓層允許平面投影重疊。
- 發現同樓層重疊時停止並列出所有衝突 Space；不使用 priority 自動選一個。
- 未命中維持單一 `EXT`，同時可能代表真正室外或漏畫 boundary。
- Coverage Report 必須列出全部 `EXT` 物件與原因：無 boundary layer、layer 無 boundary、取不到 bbox、有 boundary 但未命中；由使用者判斷，不新增第三種狀態。

### Elevation

物件 `_06_高程計算`／`_13_高程顯示` 在命中空間後＝該空間所屬高程框 `_15_樓層高程*` ＋（物件取樣 Z − 高程框曲線 Z）。`_05_高程基準` 決定取樣點（BH／TH／CH／BC）。各層畫在 Z=0、框也在 Z=0 時，就是框數字加物件高度（樓面 50、窗台 150）。找不到高程框時維持世界 Z。

未命中空間（`EXT`）時沒有高程框可對，用下表對世界座標取樣。

| Basis | 未命中空間時的取樣 |
|---|---|
| `BH` | 物件底部世界 Z |
| `TH` | 物件頂部世界 Z |
| `CH` | 天花物件底部世界 Z |
| `BC` | Block 基準點世界 Z；非 Block 直接報錯 |

幾何命中仍是 XY：底面中心，沒命中再用四角。同時命中兩個以上空間時不自動猜；Apply 會 zoom 後彈出清單點選空間名稱。已寫入且仍屬於命中集合的 `space_id` 視為已指定。

樓層歸屬靠空間框平面範圍。各層並排畫在 Z=0、用不同高程框包住，是預期畫法。上下層平面重疊會變成不明確。

1.x `TH/BH` 只由 migration 辨識，不進 2.0 enum。

## Registry

- Registry 是不可人工編輯的版本化發布快照；檔名與路徑見「檔案與路徑」。
- 根物件必須含 `schema_id: loopflow.registry`、`schema_version`、`project_id`、`registry_revision`。
- 正式發布掃描 M3D 正式範圍內全部 3D 物件，包含 hidden／locked。
- 不符合 schema 的物件仍須進報告並阻擋發布，不能先被過濾掉。
- 選取／篩選模式只作局部預覽或套用，不得宣告全專案可發布。
- Payload 只含下列 canonical 欄與明確 `extension` 物件，不快照全部 UserText。
- 寫入採 lock → 重讀 → pending → validate → atomic replace；失敗保留 last-good。唯一寫入模組是 `features/registry`（C03）。Nexus 組 payload 屬 NX-07，不得自己寫 lock 或刪正式 JSON。
- 機器可讀形狀見 `wip/fixtures/schema/registry.json`。

### 根物件

| JSON key | 型別 | 規則 |
|---|---|---|
| `schema_id` | string | 必須為 `loopflow.registry` |
| `schema_version` | integer | 目前 `1` |
| `project_id` | UUID | 與 Rhino 文件 `lf_project_id` 相同 |
| `registry_revision` | integer | 本專案從 `1` 起，發布成功才遞增 |
| `published_at` | string | ISO-8601；只作紀錄，不作身分 |
| `model_unit` | string | 發布當下的 `ModelUnitSystem` 名稱 |
| `types` | array | 當時 Dictionary 的 Type 快照，讓此 revision 不依賴事後改過的 Excel |
| `spaces` | array | 當時全部 Space Boundary；必含保留列 `EXT` |
| `objects` | array | 正式範圍內全部 3D 物件 |
| `extension` | object | 唯一允許的非核心區；未知核心欄仍阻擋 |

### `types[]`

| JSON key | 來源 |
|---|---|
| `type_id`、`type_category`、`type_sequence` | Type identity |
| `type_display_name` | Type 顯示名 |
| `layer_path` | Dictionary `__Rhino Layer` |
| `estimation_unit` | Type rule |
| `measurement_rule` | Type rule |
| `elevation_basis` | Type rule |
| `construction_default` | 僅供新 layer；不覆寫 instance |
| `remarks_default` | 僅供尚未填寫的物件 |

### `spaces[]`

| JSON key | 規則 |
|---|---|
| `space_id` | UUID；保留字 `EXT` 必存在 |
| `level_id` | UUID；`EXT` 為 null |
| `space_display` | 顯示名稱；`EXT` 顯示 `EXT` |

### `objects[]`

對齊物件 instance 核心欄。`type_display_name` 在此為發布當下快照，供 Sync 讀取，不回寫物件 UserText。`measurement_rule` 與 `estimation_unit` 只在 `types[]`。`dimension_*`、`quantity`、`local_frame` 不進 `objects[]`。

| JSON key | 規則 |
|---|---|
| `object_id` | UUID |
| `type_id`、`type_category`、`type_sequence` | 必須能在本 revision 的 `types[]` 找到 |
| `type_display_name` | 發布快照 |
| `construction_status` | instance 現值 |
| `space_id`、`space_display` | 未命中為 `EXT` |
| `elevation_basis`、`elevation_value`、`elevation_display` | 計算當時的快照 |
| `remarks` | instance override；缺值 null |
| `data_revision` | 寫入此 Registry 前的 Apply revision |

Tag、View、Drawing、Sheet 不進 Registry；Sync 用 `object_id`／Sheet metadata 去對。

## View／Drawing／Sheet

### View

- `view_id`（UserText `lf_view_id`）綁定 Section／Clipping Plane、Detail 與固定 2D↔3D transform。
- 不以名稱或可變 bbox 作正式定位身分。
- 持久化至少包含：`view_id`、`schema_id`、`schema_version`、固定 transform、來源 Section／Detail 參照。

### Drawing

- `drawing_id`（UserText `lf_drawing_id`）保存來源 `view_id`、來源 `registry_revision` 與人工狀態。
- 第一個必做能力是辨識前次產出，讓使用者選取代、新增或略過。
- Drawing element 以 `drawing_element_id` 識別，可有零個、一個或多個 `source_object_ids[]`。
- Provenance 欄位：

| JSON key | 值域 |
|---|---|
| `provenance_method` | `rhino_association`／`loopflow_geometry`／`migration` |
| `provenance_state` | `current`／`modified`／`stale`／`unindexed`／`ambiguous` |

- 人工修改後 `provenance_state` 不得仍為 `current`。
- Materialize 回報 indexed／unindexed／ambiguous 覆蓋率；索引不完整不阻擋 Drawing 產出。
- 更新不得靜默覆寫人工修改的 Drawing。

### Sheet

- `sheet_id`（UserText `lf_sheet_id`）與 metadata 是圖號、圖名、系列、樓層、區域、序號、比例與版次的真相。
- Layout 名稱由 metadata 產生，不反向拆字串取得身分。
- 比例人工輸入；canonical key 為 `scale`，與圖幅、欄位排序及顯示標籤分離。1.x 的 `03-A3 Scale` 不得沿用為 key。
- Layout ID 只寫 manifest 宣告為 `role: title_frame` 的 Block；未知 Block 報告並零寫入。
- Sheet metadata 至少包含：`sheet_id`、`drawing_no`、`drawing_name`、`series`、`level`、`zone`、`sequence`、`scale`、`issue`。圖框顯示對應 `drawing_no`／`drawing_name`／`scale`。

### Duplicate Sheet 特例

- 建立新的 `sheet_id`、`drawing_id`、`tag_id`。
- 除純手動 `TAG_DW` 外，所有 Tag 清除來源綁定、自動（`owner: render`）欄位與 `remarks_manual`，由使用者重新綁定。
- Lock 恢復該 template 的 `default_lock_state`。
- `TAG_DW` 的 `dw_id`、`dw_width`、`dw_height` 及其他 Block 內容完整保留。
- 不使用或覆蓋使用者系統剪貼簿。

## Tag Template 與 binding

機器可讀的 10 份 manifest 見 `wip/fixtures/schema/tag_templates.json`。未知 Block 報告並零寫入；只有 `role: title_frame` 可收圖號／圖名。

### 共通身分欄

每個 Tag／圖框 instance 都寫這些 UserText；不在 24 個畫面 key 內。

| JSON key | UserText key | 規則 |
|---|---|---|
| `tag_id` | `lf_tag_id` | 複製 Sheet 必發新號 |
| `template_id` | `lf_template_id` | 實際 Block 定義名，例如 `TAG_ELEV_1` |
| `template_version` | `lf_template_version` | 目前皆為 `1` |
| `binding_mode` | `lf_binding_mode` | 必須是該 template 允許的值 |
| `source_object_id` | `lf_source_object_id` | Grab／Laser |
| `target_view_id` | `lf_target_view_id` | Index |
| `target_sheet_id` | `lf_target_sheet_id` | Index |
| `source_block_name` | `lf_source_block_name` | 僅 Item |
| `lock_state` | `lf_lock_state` | 僅 `lock_allowed: true` 的 template；`true` 為鎖定 |
| `host_sheet_id` | `lf_host_sheet_id` | Tag 所在頁 |
| `last_synced_revision` | `lf_last_synced_revision` | 最近一次 Sync 使用的 Registry revision；未同步為 null |

### 10 份 manifest

| template_id | family | role | block_names | binding_mode | lock_allowed | default_lock_state |
|---|---|---|---|---|---|---|
| `TAG_HEIGHT_GRAB` | `height` | `tag` | `TAG_HEIGHT_GRAB` | `object` | true | false |
| `TAG_HEIGHT_LASER` | `height` | `tag` | `TAG_HEIGHT_LASER` | `object` | true | false |
| `TAG_FINISH_GRAB` | `finish` | `tag` | `TAG_FINISH_GRAB` | `object` | true | false |
| `TAG_FINISH_LASER` | `finish` | `tag` | `TAG_FINISH_LASER` | `object` | true | false |
| `TAG_ITEM` | `item` | `tag` | `TAG_ITEM` | `block_name` | true | false |
| `TAG_DW` | `dw` | `tag` | `TAG_DW` | `manual` | false | — |
| `TAG_SECTION_DETAIL` | `index` | `tag` | `TAG_SECTION_DETAIL` | `view` 或 `sheet` | true | false |
| `TAG_ELEV` | `index` | `tag` | `TAG_ELEV_1`～`4` | `view` 或 `sheet` | true | false |
| `TAG_ELEV_0` | `elev_key` | `tag` | `TAG_ELEV_0` | `none` | true | false |
| `SAMPLE_FRAME` | `title` | `title_frame` | `Sample_Frame` | `none` | false | — |

`lf_template_id` 寫實際 Block 名。`TAG_ELEV_1`～`4` 共用 `TAG_ELEV` 這份欄位契約，以 `block_names` 查找。

| Family | Binding | 重要規則 |
|---|---|---|
| Height／Finish Grab | `source_object_id` | 使用者直接選模型來源 |
| Height／Finish Laser | `source_object_id` | 以固定 View transform／有效 provenance 找候選；多候選由使用者選 |
| Index | `target_view_id`／`target_sheet_id` | 圖號來自**目標** Sheet metadata |
| `TAG_ELEV_0` | 無模型 binding | 六個方向／編號欄人工；`sheet_code` 來自**目前頁** |
| `TAG_DW` | `manual` | 全部欄位人工；Sync 不碰；Health 不判 unbound |
| Furniture Item | `block_name` | 同款可共用 `FF-01__Chair-1`；格式錯誤停止並警告 |
| Title Frame | `role: title_frame` | 只寫 `drawing_no`／`drawing_name`；`scale` 人工 |

家具 Block 名稱是 ECO-04 的明確例外：Health 不判 `orphaned`。若未來需要逐件追蹤，另增 instance ID，不把流水號塞進共用 Block 名稱。

### 顯示欄 owner

`owner`：`render` 可由 Sync／Layout ID 重算；`manual` 除 Duplicate Sheet 清除 `remarks_manual` 外不得覆寫；`system` 由 UI 或指令寫入。缺值顯示 `-` 只用於 `render` 欄。

| template | JSON key | UserText | owner | writer | 來源 | 複製 Sheet |
|---|---|---|---|---|---|---|
| Height | `elevation_basis` | `lf_elevation_basis` | render | infuser | 來源物件高程基準 | 清除 |
| Height | `elevation_display` | `lf_elevation_display` | render | infuser | 來源物件高程顯示 | 清除 |
| Height／Finish | `type_category` | `lf_type_category` | render | infuser | 來源物件 Type | 清除 |
| Height／Finish | `type_sequence` | `lf_type_sequence` | render | infuser | 來源物件 Type | 清除 |
| Height／Finish | `type_display_name` | `lf_type_display_name` | render | infuser | Registry Type 快照 | 清除 |
| Height／Finish／Item | `remarks_manual` | `lf_remarks_manual` | manual | none | 使用者 | **清除** |
| Item | `item_category` | `lf_item_category` | render | infuser | `source_block_name` 的 `FF` | 清除 |
| Item | `item_code` | `lf_item_code` | render | infuser | `01` | 清除 |
| Item | `item_name` | `lf_item_name` | render | infuser | `Chair-1` | 清除 |
| DW | `dw_id` | `lf_dw_id` | manual | none | 使用者 | **保留** |
| DW | `dw_width` | `lf_dw_width` | manual | none | 使用者 | **保留** |
| DW | `dw_height` | `lf_dw_height` | manual | none | 使用者 | **保留** |
| Index | `sheet_code` | `lf_sheet_code` | render | infuser | 目標 Sheet | 清除 |
| Index | `sheet_ref` | `lf_sheet_ref` | render | infuser | 目標 Sheet | 清除 |
| Index | `detail_no` | `lf_detail_no` | manual | none | 使用者 | 清除 |
| Elev 0 | `sheet_code` | `lf_sheet_code` | render | layout_id | **目前頁** Sheet | 清除 |
| Elev 0 | `dir_num` | `lf_dir_num` | manual | none | 使用者 | 清除 |
| Elev 0 | `dir_elev` | `lf_dir_elev` | manual | none | 使用者 | 清除 |
| Elev 0 | `dir_top` | `lf_dir_top` | manual | none | 使用者 | 清除 |
| Elev 0 | `dir_left` | `lf_dir_left` | manual | none | 使用者 | 清除 |
| Elev 0 | `dir_bottom` | `lf_dir_bottom` | manual | none | 使用者 | 清除 |
| Elev 0 | `dir_right` | `lf_dir_right` | manual | none | 使用者 | 清除 |
| 圖框 | `drawing_no` | `lf_drawing_no` | render | layout_id | 目前頁 `drawing_no` | 新 sheet 重算 |
| 圖框 | `drawing_name` | `lf_drawing_name` | render | layout_id | 目前頁 `drawing_name` | 新 sheet 重算 |
| 圖框 | `scale` | `lf_scale` | manual | none | 使用者 | **保留** |

Index 的 `sheet_code` 與 Elev 0 的 `sheet_code` 同名不同上下文，以 template 的 writer／來源列區分，不得混用。

### Lock

- Canonical `lock_state` 為 boolean，由 UI 切換。
- Locked Tag 凍結內容與重新綁定，但 Health 仍唯讀判斷 stale／orphaned。
- `TAG_DW` 與 `SAMPLE_FRAME` 沒有 lock 欄；不得為它們寫入 `lf_lock_state`。
- 1.x 非單一 `x/X` 的值只由 migration 列為待確認，不推測使用者意圖。
- 新建與 Duplicate 後，可鎖 template 的 `lock_state` 恢復為 `false`。

## Health 與 Repair

Health 必須唯讀產生 Issue Report；顏色只是可復原的提示層。至少表達：

```text
healthy
unbound
orphaned
stale
ambiguous
manual_locked
unindexed
unchecked
```

- 每項問題保存原因、影響、建議修復、預覽、使用者選擇與 repair result。
- Repair 不得偷偷重綁、換 ID、覆寫人工成果或刪除 Drawing。
- 報告要列出涵蓋與未涵蓋的 Tag／Block；未檢查項目不可併入通過數。

## 1.x → 2.0 名稱對照

下表只供 migration 掃描與 fixtures 對讀；日常指令不讀舊 key。

| 1.x 名稱 | 2.0 canonical | 處理 |
|---|---|---|
| `__Rhino Layer` | `layer_path` | Dictionary 顯示欄；載入後改 machine key |
| `_01_空間名稱`、`Space_Name` | `space_id`／`space_display` | 拆身分與顯示；boundary 不再只用名稱 |
| `_02_建構狀態` | `construction_default`／`construction_status` | Type 預設與 instance 分離 |
| `_03_ID編號` | `type_id`／`type_category`／`type_sequence` | 用類別碼前綴拆分，不用第一個 `-` |
| `_04_ID名稱` | `type_display_name` | 只作顯示，不寫進物件當身分 |
| `Q_01_寬度W`／`Q_02_深度D`／`Q_03_高度H` | `dimension_w`／`dimension_d`／`dimension_h` | 1.x 為 `_05`～`_07`；2.0 留在 Dictionary 給 GH，不寫物件 |
| `Q_04_單位` | `estimation_unit` | 1.x 為 `_08_單位` |
| `Q_05_計量規則` | `measurement_rule` | 1.x 為 `(單位計量規則)`；不下放物件 |
| `Q_06_實作數量` | `quantity` | 1.x 為 `_09_實作數量`；2.0 不計算 |
| `_05_高程基準` | `elevation_basis` | 1.x 為 `_10_高程基準`；2.0 enum 僅 BH／TH／CH／BC |
| `_06_高程計算` | `elevation_value`／`elevation_display` | 1.x 為 `_11_高程計算`；數值與顯示分離 |
| `_07_UUID` | `object_id` | 1.x 為 `_12_UUID`；正規化為小寫 UUID |
| `_08_備註` | `remarks_default`／`remarks` | 1.x 為 `_13_備註` |
| `_CB.01`～`_CB.04` | （無） | 不進 2.0 schema；migration 只辨識後忽略 |
| `TH/BH` | （無） | migration 列為待確認 |
| `Source_UUID` | `source_object_id` | Tag binding |
| `.Target_DV_ID` | `target_view_id`／`target_sheet_id` | 依 Index 目標種類拆 |
| `NAME_PARSED`、`.Auto_Item_*`、`.Auto_DW_ID` | （無核心欄） | migration 辨識後丟棄；改讀正式 binding |
| `Role`、`Target_CP` | （無） | 不承接；View Registration 另建 |
| `attr_Lock_不更新>寫入x或X` | `lock_state` | 僅 `x`／`X` → `true`；其他值待確認 |
| `attr_ch_key` | `elevation_basis` | Height render |
| `attr_ch_val` | `elevation_display` | Height render |
| `attr_mat_key` | `type_category` | Height／Finish render |
| `attr_mat_val` | `type_sequence` | Height／Finish render |
| `attr_note` | `type_display_name` 或 `item_name` | Height／Finish 對 Type 名；Item 對家具名 |
| `attr_manual_補充說明` | `remarks_manual` | manual；Duplicate 時清除 |
| `attr_item_key` | `item_category` | 來自 `FF-01__Chair-1` |
| `attr_item_val` | `item_code` | 同上 |
| `attr_dw_id` | `dw_id` | manual；2.0 Sync 不寫 |
| `attr_DW-W_輸入門窗寬` | `dw_width` | manual |
| `attr_DW-H_輸入門窗高` | `dw_height` | manual |
| `Category` | `sheet_code` | Index＝目標頁；Elev 0＝目前頁 |
| `REF_ID` | `sheet_ref` | Index 目標頁 |
| `Detail_NO` | `detail_no` | manual |
| `1-Elev_num` | `dir_num` | Elev 0 manual |
| `2-Elev` | `dir_elev` | Elev 0 manual |
| `3-Top` | `dir_top` | Elev 0 manual |
| `4-Left` | `dir_left` | Elev 0 manual |
| `5-Bottom` | `dir_bottom` | Elev 0 manual |
| `6-Right` | `dir_right` | Elev 0 manual |
| `DWG_NO` | `drawing_no` | 只寫 `role: title_frame` |
| `DWG_NAME` | `drawing_name` | 只寫 `role: title_frame` |
| `03-A3 Scale` | `scale` | 去掉圖幅與排序前綴；人工 |
| `FF-01__Chair-1` Block 名 | `source_block_name` | 格式不變；不當 `object_id` |
| `2D_D1`／`3D_W1` | （主鏈不使用） | 留給 F01 2D 工具 |

## Fixtures 與 schema 鎖定門檻

A03／A04 已鎖定名稱、Registry 形狀與 10 份 manifest。A06 已做成可機器驗證的 fixtures，位於 `wip/fixtures/contract/`；執行 `python wip/tools/check_contract_fixtures.py` 必須通過：

- 15 欄 Dictionary 的合法、缺值、重複、未知欄、錯單位與錯量綱案例。
- 92 筆 measurement rule 的完整 baseline，量綱 92/92 通過。
- Object ID 複製、碰撞、mapping 與 rollback。
- Space 多樓層、共邊、重疊、EXT 四種原因。
- BH／TH／CH／BC 與 legacy `TH/BH`。
- local frame：Block 插入平面、Extrusion、唯一平面、定向 Box、L 形／挖洞阻擋、沿用已存框、損壞框、禁止 World bbox。
- Drawing provenance 零／一／多來源、過期與人工修改；索引不完整不阻擋出圖。
- 24 個 Block key 與畫面擷取一致、未知 Block 零寫入、Duplicate Sheet 清除／保留。
- Registry 未知版本、未知核心欄與缺 EXT 阻擋。
